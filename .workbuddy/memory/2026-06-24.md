# 2026-06-24 工作日志

## 20:55 — 修复全部 SPM skill 路径引用问题

**问题**：安装程序通过 `mklink /J` 建 junction，AI 执行 skill 时 CWD 是用户项目目录而非 ShitPM 仓库，所有 `scripts/python/`、`references/`、`templates/`、`contracts/`、`lib/` 路径解析失败。

**方案**：每个 skill frontmatter 后新增 `## 路径解析规则` 段：
- 从 `<!-- SHITPM GLOBAL RULES START -->` 读 `$BUNDLE`
- 5 类资源路径 → `$BUNDLE` 下解析
- `.workflow/`、`output/` → CWD 相对（不变）

**修改**：9 个 skill（spm-start 无外部依赖，跳过）。alt pattern 误删 6 个 h1 标题已补回。

## 新增 spm-prototype-mark skill

## 全面代码审查与修复

已完成 13 处问题修复：

**🔴 Blocker：**
- `output/prd/prd.md`：删除泄漏的模板注释，修正状态机（移除 design 不存在的 `withdrawn`）
- `scripts/check-stage.py:122`：修复乱码 `"閫氳繃"` → `"通过"`

**🟡 代码重复：**
- 新建 `scripts/python/shared_md.py`，提取 `parse_headings`/`parse_tables_with_context` 共用
- `verify-against-metadata.py` 移入共享模块
- `stage-prep.py` 的 `parse_headings` 移入共享模块
- `STABLE_ID_PATTERN`：`review-precheck.py` 和 `anchor-verify.py` 移除 V1 不存在的 `REQ|RISK|CASE|WVR` 前缀

## 21:13 — 修复标记点随滚动飞出屏幕 bug

**根因**：`.prototypemark-mark` CSS 用 `position: absolute`，但标记 append 到 `document.body`（static），坐标来自 `getBoundingClientRect()`（视口坐标），absolute 定位不随滚动更新。

**修复**：
- CSS: `position: absolute` → `position: fixed`，使坐标与 `getBoundingClientRect()` 一致
- 在 `_createMark` 中存储 `mark._pmTargetEl` / `mark._pmAnn` 引用
- 新增 `_repositionAllMarks()` 方法，遍历所有标记重算位置
- `init()` 中注册 debounced scroll + resize 监听（50ms debounce）

## 21:09 — 修复 prototype-mark 两个运行时 bug

**Bug 1: `createShellApp is not defined`**
- 根因：`lib/shell.js` 不存在，原型 HTML 引用它后 404
- 修复：创建 `lib/shell.js`，基于 `references/prototype-writing.md` 第七章正确模板实现 `createShellApp(pages)`，使用 `<component :is>` 而非反模式
- 路径改写 regex 已验证正确：`(?<!\.\./)src="lib/` → `src="../../lib/`

**Bug 2: `Cannot read properties of undefined`**
- 根因：`inject-prototype-marks.py` 注入的 JS 中 `PanelManager._setActiveMark` 方法内 forEach 回调使用 `self` 但未在该方法作用域定义 `var self = this`
- 修复：在 `_setActiveMark` 开头添加 `var self = this;`

**附带清理：**
- 移除 `rewrite_lib_paths` 中的死代码 `replacements` 列表
- 简化并重新注明 lookbehind 注释
- 删除冗余的 `(?<!\.\./\.\./)` lookbehind（`(?<!\.\./)` 已覆盖）

**💭 其他：**
- 删除死代码 `skills/_write_skill.py`
- `contracts/metadata-anchor-rules.md`：design 文件数 11→12，补充 `field-constraints.json`
- `02-测试与验收.md` / `01-新辅助器实现总规约.md`：全部 `anchor-build.py` → `stage-prep.py`
- `review-precheck.py:70`：添加脆弱匹配的注释说明
- 删除废弃脚本 `scripts/python/check-stage.py`（主链路已用 `stage-context.py`）

## 重写 spm-prototype-mark：修复注入策略根本缺陷

基于用户实战反馈，完成以下修复：

**创建缺失的注入脚本：**
- `scripts/python/inject-prototype-marks.py`（~940 行）：纯 Python 标准库，无外部依赖
  - 剥离旧注入标记 + lib 路径改写 + 内联 CSS/JS/数据
  - 不修改 HTML 元素属性（不做 data-pm-mark 注入）
  - Review 后修复：删未用 import/变量、去 !important、补监听器清理、补 textarea 自动聚焦

**注入策略从静态 HTML 改为运行时 CSS 选择器：**
- 旧方案：在 Vue 模板源码中注入 data-pm-mark 属性 → v-for 元素被重复标记/错位
- 新方案：标注 JS 通过 `querySelectorAll(targetSelector)[targetIndex]` 运行时定位
- 天然兼容 Vue v-for 动态渲染

**更新 SKILL.md：**
- 标注数据格式从 `selector: "[data-pm-mark=...]"` 改为 `targetSelector: "css.selector"` + `targetIndex: N`
- ID 生成规则改为 `md5(targetSelector + "-" + targetIndex)`
- 新增 v-for 元素 targetIndex 确定方法、daisyUI targetSelector 参考表
- 新增防 f-string 冲突的反模式（#15）
- 自检项从 11 项精简到 8 项（移除 data-pm-mark 相关校验）

**CSS/JS 重写：**
- PanelManager/MarkerManager/PopupManager/AnnotationStore/PageObserver 全部内联
- MarkerManager 改用 `querySelectorAll` 运行时定位
- JS 使用 Python raw string（`r"""..."""`)，彻底绕过 f-string 花括号冲突

## 二次审查——补修

- `scripts/python/prd-style-lint.py:46`：STABLE_ID_PATTERN 也残留 `REQ|RISK|CASE|WVR`，已同步修复
- 全部 12 个 Python 文件编译通过，无语法错误
- `shared_md.py` 模块导入验证通过

## 修复 spm-design 和 spm-prd SKILL.md 编码问题

- **spm-design/SKILL.md**：
  - 修复失败模式表格中 tab 字符损坏 (`\templates` → `templates`) 和换行符拆行 (`\neferences` → `references`)
  - 删除稳定 ID 规则中重复的"脚本注入"短语
  - 合并两处"不要做什么"块为 9 条统一块
- **spm-prd/SKILL.md**：
  - 修复失败模式表格中同样的 tab/换行符编码损坏
- 功能定位：prototype 阶段的可选后置步骤，不进入 review 链路
- 产物：`output/prototypemark/index.html`（原型副本 + 标注系统内联）
- 标注系统包含三部分：悬浮导航栏、关键点标记、内容备注弹窗
- AI 自动读取 design.md + prd.md 生成初始标注数据（CSS selector 定位）
- 备注弹窗支持拖拽、缩放、编辑（localStorage 持久化）
- 纯 vanilla JS，不依赖外部库，所有代码内联到单个 HTML 文件
- 更新了 README.md 和 USAGE.md 补充标注阶段说明
- 用户确认方案后开始编写，方案要点：
  - AI 自动生成初始备注（非手动标注）
  - CSS selector 定位标记（非绝对坐标）
  - 不增加 review 阶段

## spm-prototype-mark review 修复（9 条）

按 review 结论修复 8 条（P1 #5 README 实际已更新，误判跳过）：

- **P0**：`scripts/python/shitpm-host.py` 的 `SKILL_NAMES` 追加 `spm-prototype-mark`，install 后才能 junction 到 `~/.workbuddy/skills/`
- **P1 #2**：selector 规则改写——默认走 `data-pm-mark` 锚点（Element Plus 原型 DOM 无 id/class 重复值）；新增步骤 5.0 明确锚点注入时机；引入 anchor 定位信息字段供 Python 脚本注入
- **P1 #3**：页面跳转策略强化——4 种机制按 spm-prototype 实际习惯排序识别；pageNav 改为 primary + fallback[] 链式结构；跳转后 400ms 校验目标页面激活；全部失败标 ⚠️ 提示手动
- **P1 #4**：MutationObserver 收窄——不监听 body，监听主体工作区容器；只监听 childList 不监听 attribute；debounce 改为 400ms；30 秒无回调降级为 popstate + 轮询
- **P2 #6**：annotationId 稳定化——`mark-<pageId>-<md5(selector).slice(0,8)>`，原型迭代重跑后 id 不变，localStorage 备注覆盖值不丢失
- **P2 #7**：弹窗单开限制保留，补设计理由说明
- **P2 #8**：与 spm-fix 关系从"无关"改为"隐含依赖"；启动时新增过期校验（对比 prototype 和 prototypemark 修改时间）
- **P2 #9**：页面级 fallback 完整规范——数据格式、标记点样式（直径 24px + P 序号）、弹窗默认 400×280px、混用规则

自检表新增 3 项：锚点注入校验、annotationId 唯一性、selector DOM 唯一匹配。

## spm-prototype skill 重构（daisyUI 替换 Element Plus）

起因：用户用 prototype skill 生成原型时遇到两个致命 bug——
1. 缺 Tailwind CSS（54 个页面有 class 无样式）
2. createShellApp 选项合并错误（extraData 展开到 data() 导致 Vue 挂载报错）

根因：skill 规则与 templates/references 互相打架——模板用 Tailwind+daisyUI，skill 要求 Element Plus 且禁止 CDN，lib/ 不存在。

用户决策：不要 CDN，用 daisyUI 替代 Element Plus。

修复内容：
- 新增 `scripts/python/download-prototype-libs.py`，下载 Vue 3 / Tailwind Play CDN / daisyUI 5（CSS+themes）到项目根 `lib/`
- `lib/` 实际 4 个文件，共 1.6MB
- `templates/prototype.html` CDN 引用改本地 lib/ 路径
- `skills/spm-prototype/SKILL.md`：
  - 组件规则改 daisyUI 5（CSS-only，无 JS 依赖）
  - 资源引用规则改"必须本地 lib/ 四件套"
  - 新增多页面 shell 写法约束（禁止 extraData 展开到 data()，必须独立组件 + `<component :is>`）
  - 自检表从 3 项扩到 7 项（新增四件套引用 / 无外部 CDN / shell 选项合并 / 无 el- 残留）
  - 失败模式表新增：lib 缺失、createShellApp 报错、无样式、误用 el-
  - 反模式表新增：使用 Element Plus、自造 createShellApp、extraData 展开到 data()
- `references/prototype-writing.md`：
  - 第三章 Element Plus 组件约定 → daisyUI 组件约定（含完整 Element Plus → daisyUI 映射表）
  - 失败模式速查表 / 反例黑名单更新
  - 新增第七章"多页面 shell 写法模板"，给出正确的单文件版本 + 多文件拆分版本 + 3 个禁止的反模式代码示例

daisyUI 5 关键认知：CSS-only 库，无 JS，交互态用 Vue 响应式变量或 tabindex + :focus-within 管理。

## spm-prototype-mark 第二轮 review（对照上游真实实现）

第一轮 review 修复后，第二轮对照 `templates/prototype.html` + `references/prototype-writing.md` 第七章的真实实现做 review，发现 5 个 P0 + 6 个 P1 + 4 个 P2：

**P0（必挂）：**
1. 组件库认知错误——SKILL 仍写 `el-button`/`el-form-item`/`el-table`/`el-tag`，实际 spm-prototype 已全面 daisyUI（`btn`/`table`/`badge` 等），自检项 7 明确"无 el- 残留"。AI 按 el- 找元素全空。
2. lib/ 相对路径断裂——prototype 引用 `lib/xxx`（相对路径），复制到 `output/prototypemark/` 后指向 `output/prototypemark/lib/`（不存在），四件套 404，标注原型白屏无样式。
3. 页面切换机制过度泛化——SKILL 列 4 种策略 + 3 级 fallback，实际 references 第七章是强制模板：`currentPage` + `<component :is>` + `go(pageKey)` 单一机制。fallback 全是死代码。
4. 自检项 9 不可执行——"Python querySelectorAll 校验"，Python 无此方法，且 Vue 模板未渲染。
5. 锚点注入脚本只给伪代码——`scripts/python/` 下无 mark 脚本，bs4 非标准库未声明依赖。

**P1：** 最小读取集合缺 references/prototype-writing.md；pageNav.verify 字段未定义；多页面拆分原型未复制 pages/ 目录；过期校验跨章节分散；备注 300 字上限与页面级示例冲突；MutationObserver 监听容器假设 `.main-content`/`.workspace`（实际是 `<main>` 内 `.flex-1.overflow-y-auto.p-6`）。

**P2：** 缺脚本路径章节；深色弹窗对比度说明不足；与 spm-prototype-review 执行顺序未说明；spm-fix 传播矩阵未更新。

Review 报告输出到工作目录 `spm-prototype-mark-review.md`。结论：不通过，必须修 P0 后再交付。等用户确认修复方案（特别是 P0-2 lib 路径改写方案 A/B/C、P0-5 脚本方案）再动手改。

## spm-prototype-mark 第二轮修复（P0/P1/P2 全部修复）

按 review 报告执行顺序修复，产物：

### P0-5: 新增 inject-prototype-marks.py
- 路径 `scripts/python/inject-prototype-marks.py`
- 统一入口，一步完成三件事：lib 路径改写（`lib/` → `../../lib/`）、data-pm-mark 锚点注入、标注系统（CSS + JS + 数据）注入
- Python stdlib only（no bs4），内嵌完整 CSS 模板和 JS 模板
- JS 模板实现全部 5 个模块：PanelManager / MarkerManager / PopupManager / AnnotationStore / PageObserver
- 支持 --dry-run / --validate-only 模式
- 功能验证通过：dry-run 测试成功注入 1 个锚点 + 路径改写 2 个引用 + JS/CSS 完整嵌入

### SKILL.md 重写（全部 P0/P1/P2 修复）
- P0-1: el- 前缀全删，全部替换为 daisyUI 类名（`btn`/`input`/`badge`/`table`/`btn-primary`/`input-bordered` 等）
- P0-2: 步骤 3 追加 lib 路径改写说明；自检表追加第 9 项 lib 路径校验；注入脚本内置路径改写
- P0-3: 删除 4 策略表 + fallback chain，改为单一 `vm.go(pageKey)` 机制；pageNav 简化为 `{ pageKey: "PageList" }`
- P0-4: 自检项 9 改为 lib 路径校验 + data-pm-mark 计数校验（删除不可行的 querySelectorAll 在 Python 中执行）
- P1-1: 最小读取集合追加 `references/prototype-writing.md`（理解 shell 的 currentPage/go/component:is）
- P1-2: pageNav 删除 verify 字段，data 格式与简化后机制一致
- P1-3: 步骤 3 改为"复制 output/prototype/ 整个目录"（含 pages/ 子目录）
- P1-4: 过期校验从"与其他 skill 的关系"移入步骤 1 前置检查表
- P1-5: 备注字数分级：元素级 50-300 字 / 页面级 100-500 字
- P1-6: PageObserver 监听容器明确为 `<main>` 或 `#app`（删除猜测性类名 `.main-content`/`.workspace`）
- P2-1: 新增脚本路径章节
- P2-2: CSS 约束补"固定深色主题，与 daisyUI 原型浅色主题无关"说明
- P2-3: 关系表补"mark 可在 review 之前/之后/独立执行，不依赖 review 通过"
- P2-4: 关系表补"spm-fix 传播矩阵当前不感知 mark 副本，mark 自己启动时做过期校验即可，后续再评估"
- 反模式表新增 3 条：引用 el- 组件、自造页面切换识别、忘记改写 lib 路径
- 硬规则新增 3 条：必须读 references/prototype-writing.md、使用 daisyUI 类名识别元素、data-pm-mark 天然保证唯一性

## spm-prototype-mark review 后最终修复（4 项 P2）

按自检报告建议修复 4 项：

- **P2-1**：悬浮栏 mouseenter 在折叠态下不再自动展开——改为只清除 hideTimer，与 click toggle 互不干扰
- **P2-2**：`detectActivePage()` 增加 Vue `vm.currentPage` 优先检测路径，避免依赖标注元素 DOM 可见性判断激活页面
- **P2-3**：删除 `_navigateToPage` 中 `[data-pm-nav]` dead code fallback（上游模板不生成此属性，永远不命中）
- **P2-4**：自检表第 5 项补操作指引——明确对比范围（`<body>` 开头到标注系统注入标记前）、回滚到步骤 3 刚复制状态重注

## spm-prototype + spm-prototype-mark 全面 review 与修复

用户要求对两个 skill 做全面 review 并修复所有问题。Review 发现 7 个问题（4 个 🔴 blocker + 3 个 🟡 suggestion）：

### 修复内容

1. **spm-prototype-mark 缺少 symlink** → 用 `mklink /j` (junction) 创建 `~/.workbuddy/skills/spm-prototype-mark` → `shitpm/skills/spm-prototype-mark`

2. **inject-prototype-marks.py data-pm-mark 计数 bug** → `validate_output` 中 `html.count("data-pm-mark=")` 会误计入 `<script>` JSON 数据中 selector 字符串。修复：新增 `_strip_script_blocks()` 排除 `<script>` 内容后再统计；自检项 7/8 均改为"非脚本区域"统计

3. **inject 脚本无重复注入保护** → 新增 `strip_previous_injection()` 检测 `INJECT_START`/`INJECT_END` 标记，存在时剥离旧注入再重新注入；`rewrite_lib_paths` 内置反二次改写保护（先还原 `../../lib/` → `lib/` 再重写）

4. **data-pm-mark 唯一性未校验** → 新增 `validate_data_pm_mark_uniqueness()`，使用 `Counter` 统计每个 id 在非脚本区域出现次数，!=1 即为失败

5. **Popup 点击外部不关闭** → JS 新增 `_bindOutsideClick` 方法，延迟绑定 `document.click` 事件，点击弹窗外部（排除 marker 和 panel）时关闭弹窗

6. **脚本路径硬编码** → spm-prototype 和 spm-prototype-mark 两个 SKILL.md 的"脚本路径"章节改为相对表述：`SKILL.md 所在文件向上 3 级即为 bundle 根`

7. **SKILL.md 步骤编号更新** → 注入步骤 5.0-5.3 改为 5.0-5.4（新 5.0 为重复注入保护）；自检表从 10 项扩到 11 项（新增注入标记唯一性检查）；失败模式表新增"重复注入"场景

### 验证

- inject-prototype-marks.py 语法检查通过
- 7 个核心函数单元测试全部通过：strip_previous_injection / no-injection / rewrite_lib_paths / idempotent / strip_script_blocks / uniqueness-valid / uniqueness-duplicate

## spm-prototype + spm-prototype-mark 自检修复（2 处不一致）

用户要求自行 review 两个 skill，发现 2 处不一致并修复：

1. **spm-prototype/SKILL.md L50**：步骤 1 资源检查表中 `lib/` 行仍引用已废弃的 `element-plus.css`，改为 daisyUI 四件套完整路径 + 补全脚本提示
2. **spm-prototype-mark/SKILL.md L152-159**：pageNav 数据格式字段名与 L426-428 不一致（`pageId`/`pageName` vs `id`/`name`），统一为 `{ id, name, pageKey }` 并与 annotations 的 `pageId` 外键一致

两处均为 P2 级别（不影响功能，AI 执行时不会读错），但修复后消除歧义。

## 21:20 — 参考原型注释 skill 重写 prototype-mark 标注系统

**参考来源**：`D:/work/AIskills/PMsuperpowers/原型注释/skill.md`

**Bug 1 修复（createShellApp is not defined）**：
- 根因：inject 脚本将 `lib/` 改写为 `../../lib/`，但 `output/prototypemark/` 和 `output/prototype/` 是**同级目录**，不是深一层。复制整个目录后 `lib/` 相对路径天然正确，改写反而破坏路径。
- 修复：移除 `rewrite_lib_paths` 函数和 `--lib-prefix` 参数，脚本不再做 lib 路径改写。

**Bug 2 修复（Cannot read properties of undefined）**：
- 根因：`getVueShell()` 中 `appEl.__vue_app__._instance.proxy` 链式访问无 null check，Vue 未加载时 `_instance` 为 undefined。
- 修复：每一步都加 null check + try/catch，返回 null 而非抛异常。

**采用参考 skill 的视觉规范**：
- 角标：amber 矩形 badge（`rgb(250, 173, 20)`，白字 10px bold，padding 1px 4px，border-radius 2px），编号 1-999
- 浮窗：浅色背景 `#f0efef`，450px 宽，border-radius 4px，弥散阴影
- 多浮窗：支持同时打开多个不同编号的浮窗（`activePopups` 对象替代单个 `activePopup`）
- Hover 触发：鼠标 hover 角标 150ms 后打开浮窗（也支持 click 兼容移动端）
- 事件隔离：所有浮窗交互 stopPropagation
- Markdown 渲染：加粗/斜体/无序列表/有序列表/引用块
- 智能定位：浮窗出现在角标左下方 8px，溢出视口自动反向

**SKILL.md 更新**：
- 新增模块化需求聚合原则（一个组件一个角标，整合相关需求）
- 新增 Workflow B 增量更新（样式锁定，仅替换内容）
- 新增步骤 5 双向追溯（需求编号写回 PRD）
- 备注格式支持 Markdown
- 移除所有 lib 路径改写相关说明
- 更新视觉规范匹配参考 skill

## 22:10 — spm-prototype-mark 终极重构（照抄参考 skill，砍脚本）

用户要求照抄 `D:/work/AIskills/PMsuperpowers/原型注释/skill.md`（68 行完美运行），重构整个 skill。

**删除**：
- `skills/spm-prototype-mark/scripts/` 整个目录（inject-prototype-marks.py 999 行 + pycache）
- `scripts/python/inject-prototype-marks.py`（root 遗留副本）

**SKILL.md 重写**：
- 498 行 → 175 行（减 65%）
- 核心变化：
  - 零脚本：AI 直接用 Edit 工具编辑副本 HTML，不依赖 Python 注入脚本
  - 视觉规范完全照抄参考 skill：amber 角标 (#f3ad14)、#f0efef 浮窗 450px、hover 打开、X 关闭、拖拽、事件隔离
  - Markdown 1:1 还原渲染
  - 角标定位：`data-pm-mark="N"` 属性 + JS `[data-pm-mark]` 选择器
  - 不反写 prd.md（与参考 skill 的核心差异）
  - 无 MutationObserver（删除，避免死循环）
  - 步骤 2 补充 lib 路径修正：`lib/` → `../../lib/`（lib 在项目根，不在 output/ 下）
  - 步骤 3 照抄参考 skill 的模块化需求聚合规范
  - Workflow A（初始化标注）+ Workflow B（增量更新）完整保留

