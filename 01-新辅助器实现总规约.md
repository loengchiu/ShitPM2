# 新辅助器实现总规约（初稿）

> 本文档是新辅助器的唯一顶层设计源。
>
> 目的不是解释理念，而是明确：
>
> 1. 什么东西用什么方式表达
> 2. 什么规则写在哪一层
> 3. 运行链路怎么走
> 4. 哪些旧项目直接抄，抄哪些文件或思路
> 5. 哪些东西明确不继承

---

## 0. 文档定位

本辅助器不是一个“大而全 PM 平台”，而是一个面向单人或小范围协作的轻量工作流辅助器。

它只解决以下问题：

1. 把原始需求稳定收敛成对齐稿
2. 把对齐稿稳定收敛成 design 基线
3. 把 design 基线稳定展开成 PRD 正文
4. 用外置机读物做追溯、反查、复查和同步修复
5. 用最小流程状态避免串项目、串阶段、读错文件

它不解决以下问题：

1. 不负责完整项目管理
2. 不负责多人协同审批流
3. 不负责内部版本管理和历史快照
4. 不负责自动推进下一阶段
5. 不负责替代 Git 的版本能力

---

## 1. 整体设计原则

### 1.1 单一职责原则

每个阶段只负责一层问题，不允许同一层职责在多个阶段重复发明：

- `启动`：只负责找到项目、识别当前阶段、准备上下文
- `对齐`：只负责确认目标、范围、边界和建设方式
- `设计`：只负责页面、字段、流程、规则、角色、状态的结构化定义
- `PRD`：只负责把 design 基线写成研发可评审的人读正文
- `原型`：只负责把 design 或 PRD 落成可看、可讨论的表现层表达
- `同步修复`：只负责变更传播，不负责重新定义需求
- `review`：只负责判断质量，不负责代写正文

### 1.2 不重复造轮子原则

旧项目中已经成熟、且你已经明确认可的能力，直接复用，不再重造：

- 需求对齐能力：优先抄 `OMP`
- design 中间层能力：优先抄 `PMFlow`
- PRD 写作能力：优先抄 `ShitPM`
- review 与 metadata anchor（机读锚点）能力：优先抄 `TestAny`

新辅助器只做“收敛”和“拼装”，不做“重新发明”。

### 1.3 规则落点唯一原则

同一条规则只能有一个主落点。

允许有“引用”或“摘要”，但不允许多处各写半句、互相打架。

示例：

- “页面正文不得写成动作流水账”  
  主落点必须在 `skills/prd/SKILL.md`
- “什么叫动作流水账”  
  可在 `references/prd-writing.md` 解释
- “如何识别动作流水账”  
  可在 `scripts/python/prd-style-lint.py` 和 reviewer 中实现

但不能：

- template 写一半
- reference 写一半
- reviewer 再写另一半

### 1.4 当前真相原则

辅助器内部只维护“当前版本”，不维护内部历史版本。

含义：

- 每个阶段的人读产物只有一份当前稿
- 每个阶段的机读物只有一套当前结构
- 后续修改直接覆盖旧稿
- 下一阶段只读取上一阶段当前稿
- 不在辅助器内部做 snapshot（快照）、manifest（清单）、revision（修订链）、历史 run 追踪

历史回滚、差异追踪、版本留痕统一交给 Git。

### 1.5 外置机读物原则

人读物和机读物分离。

- 人读物只服务人类阅读、讨论、评审
- 机读物只服务追溯、反查、lint、review、fix 传播

禁止：

- 在正文里混入内部字段名、稳定 ID、source ref
- 让模型手写大型机读物
- 用正文承担 metadata 职责

### 1.6 最小 gate（前置门槛）原则

writer 阶段前只保留最小前置检查，不把质量问题前置成门禁。

前置 gate 只处理：

1. 项目根目录是否正确
2. 当前阶段是否正确
3. 上游当前稿是否存在
4. 上游当前机读物是否存在
5. 是否串项目、串阶段

不做成前置 gate 的内容：

1. 文风是否自然
2. 页面写得是否过粗
3. 是否出现动作流水账
4. 是否缺少展示规则
5. 是否缺少异常边界

这些一律后置到 `lint + reviewer + 人工复核`。

### 1.7 规则驱动，脚本兜底原则

默认由 AI 按 skill 执行动作。

脚本只做确定性工作，不做主流程判断：

- 解析
- 校验
- 反查
- 生成机读物
- 生成 lint 结果
- 生成 reviewer 结构化结果

脚本不是主流程编排器。

### 1.8 template 只管骨架原则

template（模板）只负责：

- 文档章节层级
- 最小骨架
- 稳定结构

template 不负责：

- 写作哲学
- reviewer 规则
- 门禁
- 文风约束
- 元数据约束

### 1.9 reference 只做示例原则

reference（参考）只负责：

- 好例子
- 坏例子
- 复杂场景写法
- 对照说明

reference 不负责：

- 硬规则
- 必须遵守的执行顺序
- 前置 gate
- reviewer 判定条件

如果参考限制不住执行器，就必须把规则上移到 skill 或 reviewer。

### 1.10 质量判断后置原则

“能不能开始”与“写得好不好”必须分开。

- `status / stage context` 只判断能不能开始
- `writer skill` 只负责怎么写
- `lint / reviewer` 只负责写得好不好

不得把 reviewer 职责前置到 writer 阶段。

---

## 2. 要达成的目标

### 2.1 流程目标

新辅助器只保留最小主链路：

`启动 -> 对齐 -> 设计 -> PRD 或 prototype -> 同步修复`

其中：

- `PRD` 与 `prototype` 平级，均直接以 `design` 为基线
- `prototype` 可以先于 `PRD` 生成
- `review` 只保留关键阶段：`设计 review`、`PRD review`、`原型 review`
- `review` 不自动推进阶段
- `fix` 不单独做成复杂债务系统，而是明确为“同步修复”

### 2.2 质量目标

PRD 必须稳定避免以下问题：

1. 命中本规约 `7.2 不继承的写法问题` 中定义的坏味道
2. 页面编号重复
3. 动作整段复用
4. 缺少展示规则、状态流转、异常边界三层覆盖

### 2.3 结构目标

必须达成以下结构收敛：

1. 同一规则只有一个主落点
2. 人读物与机读物职责清晰分离
3. 不再同时维护多套 PRD 写法哲学
4. 不再让 template 承担写作规则
5. 不再让 reference 承担硬约束
6. 不再让 AI 手写大型 metadata

### 2.4 效率目标

1. 简单项目应能快速起稿，不被过重 gate 拖慢
2. 复杂项目应能稳定复查，不靠临时发挥
3. 同步修复应以局部传播为主，不整篇重写
4. 原型阶段不应再出现“只生成一个 html 却跑很久”的重链路

---

## 3. 什么东西用什么方式、写在哪

本章是“规则落点总表”。实现时必须遵守。

| 对象 | 作用 | 表达方式 | 主存放位置 | 是否可执行 | 明确不负责 |
|---|---|---|---|---|---|
| `SKILL.md` | 当前动作的硬规则、最小读取、执行顺序、停止条件 | 短、硬、可执行规则 | `skills/<stage>/SKILL.md` | 是 | 示例、长解释、历史背景 |
| `reference` | 写法示例、复杂场景、好坏对照 | Markdown 示例、说明 | `references/` | 否 | 硬规则、gate、判定逻辑 |
| `template` | 人读产物骨架 | 标题层级、章节框架 | `templates/` | 弱 | 文风、规则、门禁 |
| `reviewer checklist` | 生成后质量检查 | fail/warn/pass 检查项 | `contracts/` 或 reviewer skill | 是 | writer SOP、模板结构 |
| `lint` | 可机械识别的问题检查 | Python 脚本 | `scripts/python/` | 是 | 业务语义判断、全文重写 |
| `metadata anchor` | 人读与机读追溯、反查、同步传播 | JSON | `.workflow/metadata/` | 是 | 人读展示、正文叙述 |
| `status` | 当前阶段、当前稿路径、当前 review 结果 | JSON / YAML | `.workflow/status.*` | 是 | 文风质量判断 |
| `stage context` | 当前动作所需最小上下文 | JSON | `.workflow/runtime/<stage>/` | 是 | 复杂质量结论 |
| `writer output` | 给人读的阶段产物 | Markdown / HTML | `output/<stage>/` | 否 | 元数据职责 |
| `design metadata` | design 的结构化基线 | JSON | `.workflow/metadata/design/` | 是 | 正文可读性 |
| `review result` | 当前稿质量判定结果 | JSON | `.workflow/reviews/` | 是 | 推进阶段、改稿 |

### 3.1 skill（技能）中必须写什么

只有以下内容可以进入 `SKILL.md`：

1. 触发条件
2. 最小读取集合
3. 执行顺序
4. 硬规则
5. 输出要求
6. 停止条件

只要“不看就会跑偏”，就写进 `SKILL.md`。

示例：

- PRD 页面正文必须覆盖：
  - 界面元素与展示规则
  - 交互逻辑与状态流转
  - 异常处理与边界场景
- 禁止 `**关键动作：**` 这类标签式正文
- 禁止动作流水账

### 3.2 reference 中可以写什么

可以写：

1. 好例子
2. 坏例子
3. 简单页怎么短写
4. 复杂页怎么展开
5. 同一类页面的写法对照

不能写：

1. “必须”
2. “禁止”
3. “未满足就停止”
4. “review 必须 fail”

### 3.3 template 中只能写什么

可以写：

1. 文档章标题
2. 段落层级
3. 最小骨架

不能写：

1. `页面目标：`
2. `关键动作：`
3. `状态变化：`
4. `异常提示：`

也就是：不能诱导标签式正文。

### 3.4 reviewer 中必须写什么

reviewer 负责判断“写得够不够好”，不是判断“能不能开始”。

reviewer 至少要能判断：

1. 是否命中本规约 `7.2 不继承的写法问题` 中定义的坏味道
2. 页面是否缺展示规则
3. 页面是否缺状态变化
4. 页面是否缺异常边界
5. 页面编号是否重复
6. 是否跨节代写
7. 是否存在动作复用

### 3.5 lint 中必须写什么

lint 只抓可机械识别的问题。

必须优先覆盖：

1. 标签式正文
2. 动作流水账特征
3. 表格主导
4. 重复页面编号
5. 跨节引用
6. 机读字段泄漏
7. AI 痕迹
8. 占位符

### 3.6 metadata anchor 中必须写什么

metadata anchor 不是“完整业务事实库”，而是“足够支撑反查和同步修复的结构化索引”。

每阶段至少要有：

1. 页面列表
2. 字段定义
3. 页面与字段落点映射
4. 规则与页面/字段映射
5. 来源追溯

第一版稳定 ID 只保留以下对象：

1. `MODULE-*`
2. `PAGE-*`
3. `FIELD-*`
4. `RULE-*`
5. `FLOW-*`
6. `REL-*`

补充规则：

1. 稳定 ID 首次在 `design` 阶段生成
2. 稳定 ID 只存在于外置机读物
3. `PRD`、`prototype` 正文不得出现稳定 ID
4. 同步修复时由脚本维护稳定 ID，不靠人工手改
5. 第一版不引入 `REQ-*`、`RISK-*`、`CASE-*`、`WVR-*`

不要求：

1. 维护多版本历史
2. 维护复杂 revision 链
3. 在 workflow 内部保存快照

---

## 4. 运行链路

本章定义新辅助器的固定运行链路。

### 4.0 主链路与阶段关系

固定主链路：

`启动 -> 对齐 -> 设计 -> PRD 或 prototype -> 同步修复`

补充关系：

1. 不保留独立 `wireframe` 阶段
2. `PRD` 与 `prototype` 平级，均直接以 `design` 为基线
3. `prototype` 可以先于 `PRD` 生成
4. 若 prototype 评审反馈改变业务语义，必须先回写 `design`，再生成 `PRD`
5. 纯表现层改动允许直接修改 `prototype`
6. `review` 只保留关键阶段：
   - `设计 review`
   - `PRD review`
   - `原型 review`
7. `review` 不自动推进阶段；通过后仍由 PM 手动进入下一阶段
8. 同步修复完成后，按受影响最深阶段回对应 review 收口

### 4.1 启动

#### 目标

识别当前项目、当前阶段、当前可继续的入口。

#### 输入

1. 项目根目录
2. 当前已有产物
3. 当前状态文件

#### 输出

1. 当前阶段
2. 当前各阶段主产物路径
3. 当前各阶段主机读物路径
4. 最近一次 review 结果
5. 当前已存在的对齐阶段极简内部判断记录（如存在）
6. 下一步唯一建议

#### 只做什么

1. 找项目
2. 找状态
3. 判断下一步入口

#### 明确不做什么

1. 不做需求判断
2. 不做写作质量判断
3. 不做 reviewer 职责

### 4.2 对齐

#### 目标

确认“要做什么、不做什么、边界在哪、一期二期怎么切”。

#### 输入

1. 用户原始需求
2. 背景材料
3. 必要问答

#### 输出

1. `output/align/align.md`
2. `.workflow/metadata/align/*`
3. `.workflow/runtime/align/align-notes.json`（极简内部判断记录）

#### 只做什么

1. 目标
2. 范围
3. 边界
4. 建设方式
5. 建设类型初判（`iteration / new_build / hybrid`）
6. 待确认问题

#### 明确不做什么

1. 不展开页面和字段细节
2. 不写 PRD 正文
3. 不做原型表达

#### `align-notes.json` 职责与最小字段

`align-notes.json` 与 `metadata/align` 的关系是互补，不重叠：

- `metadata/align`：给下游读取的轻量事实索引
- `align-notes.json`：只记录对齐阶段的内部判断结论

`align-notes.json` 只保留以下字段：

1. `blocking_gaps`
2. `needs_ask_back`
3. `ask_back_reason`
4. `can_enter_design`
5. `judgement_note`
6. `last_updated_at`

禁止把以下内容重复写入 `align-notes.json`：

1. 页面清单
2. 字段清单
3. 长篇业务事实
4. 可由 `metadata/align` 表达的索引信息

与 OMP 最小上下文包的映射规则固定如下：

1. `request_summary` -> `metadata/align/index.json`
2. `solution_shape` -> `metadata/align/index.json`
3. `business_stage` -> `metadata/align/index.json`
4. `system_or_page_clues` -> `metadata/align/entities.json`
5. `material_paths` -> `metadata/align/entities.json`
6. `context_gaps` -> `metadata/align/index.json`

补充说明：

- `metadata/align` 保存“当前事实和线索”
- `align-notes.json` 保存“当前判断和是否可推进”

### 4.3 设计

#### 目标

把对齐结果结构化成模块、页面、字段、流程、规则、角色、状态、权限的稳定基线。

#### 输入

1. 当前 `align.md`
2. 当前 `metadata/align`

#### 输出

1. `output/design/design.md`
2. `.workflow/metadata/design/*`

#### 只做什么

1. 模块定义
2. 页面清单
3. 字段完整定义
4. 流程设计
5. 状态设计
6. 规则设计
7. 角色定义
8. 权限定义（细到字段级）

#### 明确不做什么

1. 不写研发级页面正文
2. 不写高保真视觉表达
3. 不新增 align 没确认的范围
4. 不把 prototype 的表现层问题直接提升为业务事实

#### 推荐骨架

`design.md` 固定按以下 9 块组织：

1. 文档概述
2. 范围与建设方式
3. 角色定义
4. 模块定义
5. 核心业务流程
6. 页面清单
7. 字段定义
8. 规则与状态定义
9. 权限定义

章节约束：

- 核心章节（必须存在）：角色定义、模块定义、页面清单、字段定义、规则与状态定义、权限定义
- 辅助章节（可选）：文档概述、范围与建设方式、核心业务流程
- reviewer 只检查核心章节是否完整，不检查章节顺序
- writer 可根据项目复杂度灵活组织辅助章节

#### 字段、权限、状态的事实源边界

`design` 是以下三类内容的唯一事实源：

1. 字段完整定义
2. 权限完整定义
3. 状态完整定义

具体要求：

1. 字段定义在 `design` 中保存完整属性
2. 权限定义在 `design` 中保存完整口径
3. 状态定义在 `design` 中保存完整口径
4. `PRD` 可为交付目的完整镜像这些内容，但不得独立改写语义

字段级权限的组织方式固定如下：

1. `字段定义` 章节只写字段业务定义，不写字段级权限表
2. `权限定义` 章节负责字段级权限
3. 字段级权限按“页面 > 角色 > 字段权限例外”组织
4. 先写默认权限规则，再写例外字段
5. 不要求把所有字段逐个平铺成巨大权限表

### 4.4 PRD

#### 目标

把 design 基线展开成研发可评审的人读规格说明。

#### 输入

1. 当前 `design.md`
2. 当前 `metadata/design`

#### 输出

1. `output/prd/prd.md`
2. `.workflow/metadata/prd/*`

#### 只做什么

1. 按页面写正文
2. 用 design 基线做约束
3. 输出完整交付镜像：
   - 详细需求说明
   - 状态机
   - 权限汇总
   - 数据字典
4. 生成 PRD anchor 机读物

#### 明确不做什么

1. 不重新定义范围
2. 不脑补 design 没确认的页面和字段
3. 不写成表格稿、动作流水账或标签式正文
4. 不把自己变成字段、权限、状态的第二事实源

#### 章节职责

1. `详细需求说明` 是字段使用区、状态落地区、权限表现区
2. `详细需求说明` 不做页面字段摘要表
3. `数据字典` 是字段主定义区，按实体分组，使用统一 9 列：
   - 字段
   - 类型
   - 长度
   - 必填
   - 默认值
   - 枚举值
   - 格式
   - 业务来源
   - 说明
4. `权限汇总` 是完整权限主展示区，集中定义：
   - 页面级权限
   - 按钮级权限
   - 字段级权限
5. `状态机` 按核心业务对象组织，集中定义状态集合、迁移、触发动作和限制条件
6. 页面正文必须把状态如何流转、权限如何影响当前页面理解和交互写清楚

#### 与 `design` 的职责边界

`PRD` 与 `design` 的关系不是“拆字段属性”，而是“事实源 + 交付镜像”：

1. `design` 定义字段、权限、状态的完整事实
2. `PRD` 为研发交付完整镜像这些内容
3. `PRD` 数据字典只保留交付所需 9 列，不带内部稳定 ID、relations、anchors、source refs
4. `PRD` 权限汇总只保留研发评审需要的权限口径，不带机读映射字段
5. `PRD` 状态机只保留研发评审需要的状态口径，不带机读映射字段
6. `PRD` 不得独立新增 `design` 中不存在的字段、权限或状态定义

#### 推荐章节顺序

`prd.md` 推荐按以下顺序组织：

1. 文档概述
2. 范围
3. 业务流程
4. 详细需求说明
5. 权限汇总
6. 数据字典
7. 状态机
8. 验收标准汇总
9. 风险与待确认

章节约束：

- 核心章节（必须存在）：详细需求说明、权限汇总、数据字典、状态机
- 辅助章节（可选）：文档概述、范围、业务流程、验收标准汇总、风险与待确认
- reviewer 只检查核心章节是否完整，不检查章节顺序
- writer 可根据项目复杂度灵活组织辅助章节

### 4.5 原型

#### 目标

把 design 或 PRD 的页面行为和信息结构表达成可看、可讨论的原型。

#### 输入

1. 当前 `design.md`
2. 如已存在，则读取当前 `prd.md`
3. 生成正式原型时默认读取：
   - 详细需求说明
   - 状态机
   - 权限汇总
   - 数据字典
4. 如 prototype 先行且存在反馈，再读取 `output/prototype/prototype-feedback.md`

#### 输出

1. `output/prototype/index.html` 或按页面拆分的原型文件
2. `.workflow/metadata/prototype/*`
3. 可选 `output/prototype/prototype-feedback.md`（自由反馈文档）

#### 只做什么

1. 展示结构
2. 状态表达
3. 交互主路径
4. 权限表现

#### 明确不做什么

1. 不重新定义业务规则
2. 不替代 PRD
3. 不引入很重的构建链

#### 补充规则

1. 第一版原型走最小原型，不追求重型系统
2. 纯表现层改动允许直接改原型
3. prototype 先行时，`prototype-feedback.md` 可自由写；AI 读取后必须先归类：
   - 表现问题
   - 语义问题
4. 若反馈改变业务语义，必须先回写 `design`，再继续生成 `PRD` 或同步原型

#### `prototype-feedback.md` 传播路径

AI 读取 `prototype-feedback.md` 后，必须先输出归类结果，再决定传播：

1. 表现问题
   - 只修改 `prototype`
   - 同步更新 `metadata/prototype`
   - 不回写 `design`
2. 语义问题
   - 先回写 `design`
   - 同步更新 `metadata/design`
   - 再视影响范围重生 `PRD` 或 `prototype`

禁止行为：

1. 不得把语义问题只留在 `prototype` 闭环
2. 不得跳过归类直接修改

#### `prototype-feedback.md` 归类输出格式

AI 在开始修改前，必须先输出一轮固定格式的归类结果：

```text
原型反馈归类结果：

一、表现问题
- [页面/区域] 问题描述
- [页面/区域] 问题描述

二、语义问题
- [页面/区域] 问题描述
- [页面/区域] 问题描述

三、处理建议
- 仅改 prototype：
  - ...
- 先回写 design 再同步：
  - ...
```

格式要求：

1. 必须先分成“表现问题”和“语义问题”两类
2. 每条至少写清：
   - 所属页面或区域
   - 简短问题描述
3. `处理建议` 必须明确分流：
   - 哪些只改 `prototype`
   - 哪些必须先回写 `design`
4. 若某一类为空，保留标题并写“无”
5. 未输出这轮归类结果前，不得开始修改任何文件

### 4.6 同步修复

#### 目标

当用户修改上游或当前阶段内容时，把影响沿链路传播到当前真相。

#### 输入

1. 修改指令
2. 当前阶段稿件
3. 当前机读锚点

#### 输出

1. 覆盖后的当前稿
2. 覆盖后的当前机读物

#### 只做什么

1. 允许从任意阶段发起修复
2. 先完成修复归类与影响判断
3. 再按受影响链路逐层覆盖
4. 只改必要内容

#### 最小判断清单

同步修复不得靠 AI 自行发挥，至少按以下顺序执行：

1. 读取用户修改指令
2. 判断修改指向的对象：
   - 目标 / 范围 / 建设方式
   - 模块 / 页面 / 字段 / 规则 / 流程 / 状态 / 权限
   - 表现层布局 / 样式 / 视觉交互
3. 判定问题归属层
4. 判定事实源所在阶段
5. 判定受影响的最深阶段
6. 生成修复顺序并执行

若无法判断归属层，先停在澄清，不得直接改下游。

#### 修复完成后的 review 建议

fix skill 完成覆盖后，必须输出一条 review 建议：

1. 自动判断受影响最深的阶段
2. 输出格式：`建议进入 [阶段] review，检查对象：[对象列表]`
3. 不自动执行 review，由 PM 手动确认后触发
4. 与"review 不自动推进阶段"原则一致

#### 明确不做什么

1. 不整篇重写
2. 不制造多版本并存
3. 不回避影响分析直接硬改下游
4. 不允许逆向定源

---

## 5. 阶段职责与文件建议

本章定义建议目录结构。新仓创建时按此落地。

```text
<新辅助器仓库>/
  skills/
    start/SKILL.md
    align/SKILL.md
    design/SKILL.md
    prd/SKILL.md
    prototype/SKILL.md
    fix/SKILL.md
    design-review/SKILL.md
    prd-review/SKILL.md
    prototype-review/SKILL.md

  references/
    align-writing.md
    design-writing.md
    prd-writing.md
    prd-writing.profile.json
    prototype-writing.md

  templates/
    align.md
    design.md
    prd.md
    prototype.html

  contracts/
    review-checklist.md
    metadata-anchor-rules.md
    fix-propagation-rules.md

  schemas/
    status.schema.json
    align-notes.schema.json
    design-metadata.schema.json
    prd-metadata.schema.json
    prototype-metadata.schema.json
    review-result.schema.json

  scripts/python/
    stage-context.py
    stage-prep.py
    prd-style-lint.py
    review-precheck.py
    anchor-build.py
    anchor-verify.py

  docs/
    01-新辅助器实现总规约.md
    02-测试与验收.md
```

`references/prd-writing.profile.json` 的职责固定为：

1. 提供 PRD 写作的轻量硬约束摘要
2. 作为 writer 的快速写法 profile
3. 用于约束正文颗粒度、表格使用边界、禁止表达、字段/状态/异常覆盖要求

它不是：

1. 完整写法说明
2. 示例集
3. reviewer 报告

`review-precheck.json` 和 `stage-context.py` / `stage-prep.py` 的详细定义见 §5.5 和 §5.6。

### 5.1 人读产物建议

```text
output/
  align/align.md
  design/design.md
  prd/prd.md
  prototype/index.html
  prototype/prototype-feedback.md
```

每阶段只保留当前一份。

### 5.2 机读产物建议

```text
.workflow/
  status.json
  runtime/
    <stage>/
      stage-context.json
      review-precheck.json
      lint.json
    align/
      align-notes.json

  metadata/
    align/
      index.json
      entities.json
      relations.json
    design/
      index.json
      entities.json
      relations.json
      modules.json
      pages.json
      fields.json
      rules.json
      states.json
      permissions.json
    prd/
      index.json
      entities.json
      relations.json
      page-anchor.json
      rule-anchor.json
      field-anchor.json
    prototype/
      index.json
      page-map.json
```

每阶段只保留当前一套。

补充说明：

1. `design.md`、`prd.md`、`prototype` 是人读当前稿
2. `fields.json`、`permissions.json`、`states.json` 等是同一当前真相的机读镜像
3. 人读稿与机读镜像必须 100% 映射一致
4. 机读镜像不得新增人读稿中不存在的业务事实
5. 这不违反“当前真相原则”，因为两者不是两个版本，而是同一版本的两种视图

`metadata/align` 的文件职责固定如下：

1. `index.json`
   - 保存对齐阶段的总索引
   - 至少包含：
     - `request_summary`
     - `solution_shape`
     - `business_stage`
     - `context_gaps`
2. `entities.json`
   - 保存对齐阶段的线索对象和已确认对象
   - 至少覆盖：
     - `system_or_page_clues`
     - `material_paths`
     - 已确认角色 / 场景 / 关键对象的轻量索引
3. `relations.json`
   - 保存来源关系、承接关系、线索到对象的映射关系
   - 不复述长正文

对应 schema（结构契约）统一放在 `schemas/` 目录，至少包括：

1. `schemas/status.schema.json`
2. `schemas/align-notes.schema.json`
3. `schemas/design-metadata.schema.json`
4. `schemas/prd-metadata.schema.json`
5. `schemas/prototype-metadata.schema.json`
6. `schemas/review-result.schema.json`

### 5.3 状态文件最小字段

`status.json` 只保留以下 6 类字段：

1. `current_stage`
2. `artifacts`
3. `metadata_paths`
4. `latest_reviews`
5. `align_notes`
6. `next_recommended`

不保留：

1. revision 历史
2. snapshot 记录
3. run 链
4. 债务链
5. 多轮历史

### 5.4 review 结果产物

每个关键阶段 review 固定输出两份结果：

1. 机读结果  
   - 供脚本、同步修复、后续阶段读取
2. 简短人读摘要  
   - 供 PM 快速看结论

review 结论只允许 3 档：

1. 通过
2. 有问题需修改
3. 阻塞，不能继续

机读结果 JSON 最小字段固定为：

1. `stage`
2. `verdict`
3. `issues`
4. `issue_layer`
5. `affected_objects`
6. `needs_upstream_sync`
7. `next_recommended`

人读摘要 Markdown 最小骨架固定为：

1. 结论
2. 主要问题
3. 是否需要回上游
4. 下一步建议

### 5.5 `stage-context.py` 与 `stage-prep.py` 的边界

两个脚本职责固定分开：

1. `stage-context.py`
   - 负责“当前能不能开始”
   - 负责读取状态、判断当前阶段、收集最小读取集合、给出下一步建议
   - 不生成业务机读物
2. `stage-prep.py`
   - 负责“当前阶段的机读镜像怎么生成”
   - 负责从当前人读稿中抽取并生成 metadata anchor
   - 不判断是否允许进入该阶段

一句话区分：

- `stage-context.py` 管准入和上下文
- `stage-prep.py` 管机读镜像生成

### 5.6 `review-precheck.json`

`review-precheck.json` 是 reviewer 开始前的确定性预检查结果。

职责固定为：

1. 记录当前阶段 reviewer 读取的是哪份人读稿和哪套机读物
2. 记录脚本已完成的结构检查结果
3. 提前暴露阻塞 reviewer 的缺口
4. 告诉 reviewer 本轮应重点做人审，还是先回上游补结构

它不是：

1. 最终 review 结论
2. 人读摘要
3. 第二份 reviewer 报告

最小字段固定为：

1. `stage`
2. `artifact_path`
3. `metadata_paths`
4. `deterministic_checks`
5. `blocking_issues`
6. `warnings`
7. `can_start_review`
8. `recommended_focus`

字段含义：

- `deterministic_checks`：脚本能确定判断的检查项结果，如文件存在、结构完整、索引可解析、必要对象是否齐全
- `blocking_issues`：会阻止 reviewer 继续的结构性问题
- `warnings`：不阻止 reviewer 开始，但需要 reviewer 注意的前置问题
- `can_start_review`：当前是否允许进入 reviewer
- `recommended_focus`：建议 reviewer 重点审查的方向，如”正文写法””字段落点””权限一致性””状态流转”

### 5.7 contracts 文件职责

`contracts/` 目录存放结构契约，职责固定如下：

1. `review-checklist.md`
   - 定义每个阶段 reviewer 的检查项清单
   - 检查项引用 §7.2 坏味道定义和 §3.4 reviewer 必须判断的内容
   - 是 reviewer skill 的执行依据，不是 writer 的写作指导
2. `metadata-anchor-rules.md`
   - 定义 metadata anchor 的生成规则、稳定 ID 前缀规范、关系建模规范
   - 是 anchor-build.py 和 anchor-verify.py 的规则来源
   - 引用 §3.6 和 §5.2 中已定的 anchor 规则
3. `fix-propagation-rules.md`
   - 定义同步修复的传播规则：影响分析方法、覆盖顺序、最小判断清单
   - 是 fix skill 的执行依据
   - 引用 §4.6 中已定的修复规则

这三个文件都是”规则落点唯一原则”的主落点之一，实现时必须先写 contracts 再写 skill。

---

## 6. 旧项目继承映射

本章定义“指定什么流程抄哪个，抄哪些，要细到具体的”。

### 6.1 启动链路

| 新辅助器模块 | 主要来源项目 | 直接照抄 | 允许改造 | 明确不抄 |
|---|---|---|---|---|
| 启动 / stage context | `D:\work\PMFlow` | 项目状态识别思路、阶段上下文思路、最小读取集合思路 | 精简状态字段，只保留当前真相 | 复杂 gate 表、revision 链、snapshot 体系 |

#### 建议直接参考

- `D:\work\PMFlow\scripts\python\pmflow-stage-context.py`

抄的是思路，不是把现有文件直接搬过去不动。

要保留的只有：

1. 当前阶段识别
2. 上游当前稿路径识别
3. 上游当前机读物路径识别
4. 下一步建议生成

### 6.2 对齐链路

| 新辅助器模块 | 主要来源项目 | 直接照抄 | 允许改造 | 明确不抄 |
|---|---|---|---|---|
| 对齐 | `D:\work\AIskills\OhMyPm` | 需求对齐方法、边界确认方式、阶段问答方式、建设类型初判方式、一期二期拆分方式 | 输出格式适配新辅助器目录；状态文件与目录名改成新辅助器命名 | 与 design / PRD 重复的展开内容；Omp 内部 methods sidecar 的完整形态照抄 |

#### 必抄文件

1. `D:\work\AIskills\OhMyPm\skills\omp-disc\SKILL.md`
2. `D:\work\AIskills\OhMyPm\contracts\intake-context.md`
3. `D:\work\AIskills\OhMyPm\contracts\ask-back.md`
4. `D:\work\AIskills\OhMyPm\contracts\work-methods.md`

#### 直接照抄的内容

1. `omp-disc` 的最小读取顺序：
   - 先读状态
   - 再读最小项目记忆
   - 再补最少 contracts
   - 只在复杂场景补读 reference
2. `intake-context.md` 的最小上下文包契约：
   - `request_summary`
   - `solution_shape`
   - `business_stage`
   - `system_or_page_clues`
   - `material_paths`
   - `context_gaps`
3. `solution_shape` 的三分法和判断口径：
   - `iteration`
   - `new_build`
   - `hybrid`
4. `ask-back.md` 的追问边界：
   - 只为真实阻塞追问
   - 一次只追一个问题
   - 先查资料，查不出再问 PM
   - 最后一行必须收口为唯一问题
5. `work-methods.md` 的“生成前先做内部推导，再产出正文”思路

#### 抄法要求

1. 只抄“对齐方法”和“提问纪律”，不抄 OMP 的完整多阶段产物体系
2. 新辅助器的对齐阶段只保留：
   - 当前需求一句话总结
   - 建设类型初判
   - 业务阶段
   - 现有线索
   - 资料路径
   - 当前缺口
   - 当前轮问题或当前轮结论
3. 不照抄 `.ohmypm/` 命名，也不照抄 `output/disc / output/solution` 目录命名
4. 不把 `work-methods.md` 的完整 sidecar（旁挂文件）体系直接搬进新辅助器；只保留“先推导、再产出、分叉先问 PM”的原则
5. 新辅助器中的 `align` 不是 OMP 的原样复刻，而是吸收它的最小上下文包、建设类型判断和 ask-back 纪律

### 6.3 设计链路

| 新辅助器模块 | 主要来源项目 | 直接照抄 | 允许改造 | 明确不抄 |
|---|---|---|---|---|
| design | `D:\work\PMFlow` | design 作为稳定中间层的定位；页面、字段、流程、规则、角色、状态的结构化思路 | metadata 结构统一精简为 JSON；减少历史包袱 | 旧 YAML 分片、AI 手写 metadata、复杂阶段债务系统 |

#### 建议直接参考

- `D:\work\PMFlow\skills\pm-design\SKILL.md`
- `D:\work\PMFlow\scripts\python\pmflow-stage-prep.py`

抄的是：

1. design 必须是 PRD 的稳定上游
2. design 负责“结构化定义”，不负责 PRD 正文
3. 机读物服务于下游 PRD 和 review

### 6.4 PRD 写作链路

| 新辅助器模块 | 主要来源项目 | 直接照抄 | 允许改造 | 明确不抄 |
|---|---|---|---|---|
| PRD writer | `D:\work\AIskills\ShitPM` | 页面按用户动作组织、场景覆盖自检、自然规格说明写法、PRD review checklist、PRD 模板骨架 | 去掉旧项目专属 AI 锚点区；骨架适配新辅助器 | 模板诱导标签腔、旧项目专属编号映射区、过长附录 |

#### 必抄文件

1. `D:\work\AIskills\ShitPM\contracts\prd-generation-rules.md`
2. `D:\work\AIskills\ShitPM\contracts\prd-review-checklist.md`
3. `D:\work\AIskills\ShitPM\templates\prd.md`

#### 抄的重点不是全文，而是这些原则

1. 页面内部按用户动作组织
2. 但不能写成动作流水账
3. 每个动作写完后做场景覆盖自检
4. 表格只用于天然映射内容
5. 少用加粗
6. 不用标签式正文
7. 规格说明优先，少讲原因，多写规则

#### 明确裁决

对 `ShitPM` 的继承规则必须写死：

1. `prd-generation-rules.md`：直接抄规则
2. `prd-review-checklist.md`：直接抄检查思路
3. `templates/prd.md`：只抄章节骨架，不抄会诱导标签式正文的小节写法

也就是：

- 抄规则
- 抄检查项
- 抄大章节骨架
- 不抄 `**关键动作：**`、`**状态变化：**` 这类标签式正文写法

### 6.5 PRD 细节增强参考

| 新辅助器模块 | 主要来源项目 | 直接照抄 | 允许改造 | 明确不抄 |
|---|---|---|---|---|
| PRD 细节颗粒度 | `D:\work\AIskills\Kira-product-monster-skills` | 界面元素与展示规则、交互逻辑与状态流转、异常处理与边界场景这三层覆盖要求 | 精简冗长说明，提炼成 PMFlow/新辅助器可执行规则 | 过重、全场景穷举式写法；不适合你节奏的长篇展开 |

#### 必参考文件

1. `D:\work\AIskills\Kira-product-monster-skills\skills\Kira-product-monster-prd\SKILL.md`

#### 抄的重点

不是抄它整份长规则，而是把以下三层写成新辅助器的硬约束：

1. 界面元素与展示规则
2. 交互逻辑与状态流转
3. 异常处理与边界场景

### 6.6 review 与 metadata anchor 链路

| 新辅助器模块 | 主要来源项目 | 直接照抄 | 允许改造 | 明确不抄 |
|---|---|---|---|---|
| review + metadata anchor | `D:\work\AIskills\testany-agent-skills\plugins\testany-eng` + `D:\work\AIskills\ShitPM` | metadata anchor 反查思路、稳定 ID 体系、relation（关系）建模、trace-lint（追溯校验）契约、写作质量 checklist | 输出格式适配新辅助器；metadata 改成外置 JSON；review 结果适配新辅助器目录 | PRD 内嵌大段 `TRACEABILITY-METADATA` block；过重独立读取链；过度复杂的历史绑定逻辑 |

#### 必抄文件

1. `D:\work\AIskills\testany-agent-skills\plugins\testany-eng\skills\prd-reviewer\SKILL.md`
2. `D:\work\AIskills\testany-agent-skills\plugins\testany-eng\references\traceability-schema\traceability-schema-v1.md`
3. `D:\work\AIskills\testany-agent-skills\plugins\testany-eng\references\traceability-schema\trace-lint-contract-v1.md`
4. `D:\work\AIskills\testany-agent-skills\plugins\testany-eng\references\traceability-schema\prd-profile-v1.example.yaml`
5. `D:\work\AIskills\testany-agent-skills\plugins\testany-eng\scripts\trace_lint.py`
6. `D:\work\AIskills\ShitPM\contracts\prd-review-checklist.md`

#### 直接照抄的内容

1. `traceability-schema-v1.md` 的 canonical envelope（标准包络）思路：
   - `schema`
   - `artifact`
   - `entities`
   - `relations`
   - `waivers`
2. 稳定 ID 的建模原则：
   - 先定义对象前缀
   - 保持 ID 稳定
   - 关系独立建模
   - 不把 ID 暴露到人读正文
3. “关系独立建模，不把下游桶塞回上游对象”这一原则
4. `trace-lint-contract-v1.md` 的校验顺序思路：
   - 提取校验
   - envelope 校验
   - artifact 校验
   - entities 校验
   - relations 校验
   - waivers 校验
   - profile 校验
5. `prd-reviewer` 中“review 先跑脚本，再做人工判断”的顺序
6. `ShitPM` 的人类可读写作质量检查项

#### 抄法要求

1. 新辅助器不照抄 TestAny 的“元数据内嵌在 PRD 正文里”做法
2. TestAny 的 schema、稳定 ID 建模原则、relation 设计、lint 校验顺序直接照抄到外置机读物体系
3. reviewer 采用“两段式”：
   - 先跑外置 metadata 的 lint / anchor 校验
   - 再做人读正文质量审查
4. review 的写作质量部分优先抄 `ShitPM`
5. review 的追溯、稳定 ID、关系合法性部分优先抄 `TestAny`
6. 不照抄 `testany-eng` 当前以 Markdown 内嵌 metadata 为前提的交付形式；新辅助器统一改为外置 JSON
7. 第一版实际落地的稳定 ID 以本规约第 3.6 节为准，不直接照搬 TestAny 的全量对象前缀

### 6.7 原型链路

| 新辅助器模块 | 主要来源项目 | 直接照抄 | 允许改造 | 明确不抄 |
|---|---|---|---|---|
| prototype | 现有 PMFlow 原型经验 + 后续你认可的最简实现 | 原型作为与 PRD 平级的表现层交付物；prototype-feedback 先归类再处理的思路 | 彻底简化为轻量表达链，不再做重型生成 | 把 prototype 固定成 PRD 的唯一后继；长时间预读、复杂 style reference 链、超重 renderer 流程 |

#### 当前原则

原型阶段先不作为第一优先级重资产建设。

先保证：

1. 链路位置清楚
2. 输入输出清楚
3. 不再出现极慢的重链路
4. prototype 可先于 PRD 生成，但不改变 design 的事实源地位

### 6.8 同步修复链路

| 新辅助器模块 | 主要来源项目 | 直接照抄 | 允许改造 | 明确不抄 |
|---|---|---|---|---|
| fix / sync repair | `PMFlow` + `TestAny` | 变更影响分析、基于 metadata anchor 的传播思路、从任意阶段发现问题再回事实源同步的思路 | 简化为当前真相覆盖链 | fix debt（修复债务）系统、过重状态债务机制、下游直接定上游事实 |

#### 抄的重点

1. 改了上游，要能知道哪些下游受影响
2. 传播时只改必要点
3. 不靠人工全局 grep（全局搜索）兜底

---

## 7. 明确不继承的东西

本章是新辅助器必须明确废弃的内容。

### 7.1 不继承的流程复杂度

1. 过重前置 gate
2. writer 前的质量门禁
3. 自动推进下一阶段
4. 内部历史版本管理
5. 多套并行状态链
6. 独立 wireframe 阶段

### 7.2 不继承的写法问题

1. 纯表格式页面正文
2. 动作流水账
3. 标签式正文：
   - `**页面目标：**`
   - `**关键动作：**`
   - `**状态变化：**`
   - `**异常提示：**`
4. 过多加粗
5. 模板腔
6. 模糊表述：
   - “按配置”
   - “按规范”
   - “同常规”
   - “待补充”

补充定义：

- “纯表格式页面正文”指页面主要信息依赖字段表、区域表或动作表承载，自然语言正文不足以支撑评审。
- “动作流水账”指只按点击顺序描述动作过程，缺少展示规则、状态流转或异常边界中的关键层。
- “标签式正文”指用 `**页面目标：**`、`**关键动作：**`、`**状态变化：**` 等加粗标签段拼接正文。

### 7.3 不继承的 metadata 方案

1. AI 手写大型 metadata
2. 旧 YAML 分片主路径
3. 人读机读混写
4. 为了历史版本而保留的复杂 revision 结构
5. 把 metadata 做成第二份 design / PRD / prototype 正文

### 7.4 不继承的结构重复

1. skill、reference、template、reviewer 同时写一条规则
2. 不同项目各自维护一套 PRD 哲学
3. 在多个层级重复定义相同检查项

---

## 8. 第一轮开发顺序建议

为了保证新辅助器干净，第一轮开发顺序必须固定。

### 8.1 先写文档，不先写代码

先完成：

1. 本文档 `01-新辅助器实现总规约.md`
2. `02-测试与验收.md`

在这两份文档没定稳之前，不进入正式实现。

### 8.2 第一批必须先落的文件

1. `skills/align/SKILL.md`
2. `skills/design/SKILL.md`
3. `skills/prd/SKILL.md`
4. `skills/design-review/SKILL.md`
5. `skills/prd-review/SKILL.md`
6. `skills/prototype/SKILL.md`
7. `skills/prototype-review/SKILL.md`
8. `references/prd-writing.md`
9. `templates/prd.md`
10. `scripts/python/prd-style-lint.py`
11. `scripts/python/stage-context.py`

### 8.3 第一轮暂不追求的内容

1. 完整原型系统
2. 多人协同
3. 历史版本留痕
4. 自动化推进
5. 花哨安装器
6. 复杂多分支状态引擎

### 8.4 第一轮完成标准

只要能稳定做到以下几点，就算第一轮成立：

1. `align -> design -> prd` 能跑通
2. `align -> design -> prototype` 能跑通
3. PRD 不再退化成表格稿、动作流水账、标签式正文
4. metadata anchor 可支撑 review 和同步修复
5. 所有规则落点清楚，不互相打架

---

## 9. 当前仍需补充的空位

下列内容尚未确认，后续补到本文档即可：

### 9.1 原型链路待补充

1. 原型是否保留单文件 HTML 优先策略
2. 原型 reviewer 的最小检查项清单

### 9.2 新辅助器仓库命名待补充

1. 仓库名
2. 顶层目录名
3. `.workflow/` 是否改名

---

## 10. 一句话总纲

新辅助器不是把旧项目拼起来，而是：

> 用 `OMP` 做对齐，  
> 用 `PMFlow` 做 design 和最小流程编排，  
> 用 `ShitPM` 做 PRD 写作，  
> 用 `TestAny` 做 review 和 metadata anchor，  
> 再把这四层收敛成一个只维护“当前真相”、以 `design` 为唯一事实基线、允许 `PRD` 与 `prototype` 平级生成的干净辅助器。
