# PRD 研发交付完整性优化计划

> 状态：已确认，采用方案 B（Design 防缺失 + PRD 防丢失 + 检查防回退）
> 确认日期：2026-07-28

## 0. 背景与边界

- 研发最终以 `output/prd/prd.md` 作为产品需求交付物，不要求研发阅读 `design.md`。
- `design.md` 是轻量但完整的产品事实基线和中间审计文档，用于约束 PRD 与 Prototype 不偏离，不展开研发技术实现细节。
- 本次目标是消除因产品规格缺失产生的可避免回问，不承诺消除 API、数据库、部署、缓存、HTTP 状态码、加密算法等技术设计讨论。
- 未决事项继续记录在 `decision-notes.md`，由产品评审会口头说明；本次不改变未决事项链路，也不要求 PRD 正文新增待确认章节。
- 不引入 Schema 或其他中间文件作为新的产品事实源。
- 不改变 PRD 的“模块 → 小模块 → 页面 → 动作”整体组织方式，不改变 Prototype 的 HTML + Vue + Tailwind + daisyUI + 本地 lib 架构。

## 1. 优化目标

### G1：Design 防缺失

Design 保持人类可读和轻量，但在首次写入前发现会影响下游实现的高影响产品缺口，包括动作结果、状态副作用、唯一性、数据生命周期、跨系统责任和实际适用的质量约束。

### G2：PRD 防丢失

PRD 完整承接 Design 中已确认的目标、范围、场景、字段、页面落点、规则、状态、权限、数据范围、异常、系统边界和验收事实，不改变允许/禁止、局部/全局、条件/无条件等语义。

### G3：PRD 研发可执行

只要对应场景存在，PRD 必须让研发能够确定动作条件与结果、字段约束、状态变化、数据副作用、列表默认行为、文件与导入导出、批量操作、重复与并发的产品结果、跨系统失败结果和验收口径。

### G4：检查防回退

由 Skill、reference、模板、确定性脚本、Review 清单和固定回归样例共同防止回退；程序只阻断能够可靠证明的结构、覆盖和明确冲突，业务合理性继续由分析协议和 Review 判断。

## 2. 信息责任边界

| 信息类型 | Design | PRD | 技术方案 |
|---|---|---|---|
| 产品目标、非目标、用户场景 | 简要确认 | 简短展开 | 不需要 |
| 业务对象、关系、唯一性 | 必须确认 | 研发可执行地展开 | 设计存储方式 |
| 状态、迁移条件、业务副作用 | 必须确认 | 详细展开 | 事务实现 |
| 角色、操作权限、数据范围 | 必须确认 | 详细展开 | 鉴权实现 |
| 字段业务定义 | 必须确认 | 完整交付 | 数据库/API 映射 |
| 页面与字段落点 | 必须确认 | 按页面动作表达并保留内部字段 | 前端组件实现 |
| 列表排序、分页、加载反馈 | 业务特殊时确认 | 必须给出具体口径或引用公共默认 | 查询优化 |
| 重复点击和并发的产品结果 | 必须确认业务结果 | 展开用户行为 | 幂等、锁和版本控制 |
| 跨系统责任和失败后的业务结果 | 必须确认 | 详细展开 | 接口、重试和队列实现 |
| 产品级性能、安全、审计、兼容要求 | 实际适用时确认 | 可验收地展开 | 技术实现方案 |
| HTTP 状态码、API、数据库、加密算法 | 不写 | 不写 | 研发负责 |

## 3. 实施批次

### 批次 0：建立缺陷基准

建立或复用三类测试样例：

1. 简单业务：字段约束、唯一性、列表、编辑、提交、撤回、归档、导出。
2. 状态权限：审批、驳回、撤回、重新提交、数据范围、字段权限例外、职责分离。
3. 复杂交付：文件、导入导出、批量操作、外部系统、超时、重复、部分成功和补偿。

覆盖三类缺陷：Design 有但 PRD 丢失、Design 与 PRD 都缺失、PRD 改写 Design 语义。

### 批次 1：Design 防缺失

修改：

- `references/design-analysis-protocol.md`
- `references/design-quality-rubric.md`
- `skills/spm-design/SKILL.md`
- `contracts/design-review-checklist.md`
- 必要时局部调整 `templates/design.md`

对实际存在的关键动作检查：执行角色和前置条件、影响对象和字段、成功/失败业务结果、状态与副作用、回退路径、唯一性、重复和并发的产品结果、删除/作废/归档/恢复、时间边界、跨系统失败和实际适用的非功能约束。

不增加机械固定章节，不要求 Design 写分页数量、加载动画、HTTP 状态码、API、数据库或不适用的空 NFR 章节。

### 批次 2：PRD 防丢失

修改：

- `skills/spm-prd/SKILL.md`
- `references/prd-writing-rules.md`
- `templates/prd.md`

在首次写入前建立 Design → PRD 承接矩阵，逐项覆盖：目标/非目标、用户场景、范围和边界、模块/页面/动作、字段全部属性、页面落点和内部字段、权限和数据范围、状态和副作用、业务规则、异常补偿、实际适用的非功能约束和验收条件。

字段表调整为：

```text
| 字段 | 类型 | 必填 | 取值约束 | 默认值 | 业务来源 | 说明 |
```

其中“取值约束”承载长度、完整枚举、数值范围、格式、精度、文件类型和大小。

每个 Design 字段必须在 PRD 中归属于页面展示、页面输入、查询筛选、动作依赖或系统内部字段之一。

### 批次 3：场景覆盖补强

修改：

- `references/prd-scene-checklist.md`
- `contracts/prd-review-checklist.md`

在现有界面、交互、异常和边界检查基础上增加：

- 动作前置条件、业务副作用、重复执行和恢复；
- 数据新建、修改、删除、作废、归档、恢复和历史；
- 列表默认查询、筛选、排序、分页、跨页选择和刷新；
- 文件格式、大小、数量和失败结果；
- 导入导出范围、字段、权限、重复和部分成功；
- 批量操作上限、部分失败和结果反馈；
- 重复点击、网络重试、多窗口编辑、过期数据和版本冲突的产品结果；
- 跨系统事实源、同步方向、失败、补偿和最终责任。

不存在的场景跳过，不机械生成无关内容。

### 批次 4：检查防回退

修改：

- `scripts/python/prd-consistency-check.py`
- 按实际需要修改 `scripts/python/prd-style-lint.py`
- 按实际需要修改 `scripts/python/review-precheck.py`
- 相关测试脚本与测试数据

确定性检查优先覆盖：字段长度、默认值、枚举、格式、业务来源、内部字段、字段交付归宿、页面/模块/角色/状态遗漏和明确的权限允许/禁止反转。

复杂权限、业务合理性、状态恢复完整性和补偿合理性继续交由 Review，不用脆弱的关键词规则冒充确定性判断。

### 批次 5：回归验收

验证：

1. 简单样例能够直接确定唯一性、编辑限制、撤回、归档、导出、字段约束和内部字段。
2. 状态权限样例能够确定状态进出、驳回/撤回/重新提交、操作人、数据范围、状态副作用和非终态出路。
3. 集成样例能够确定系统责任边界、外部失败结果、部分成功、补偿、人工入口和最终责任。
4. 脚本能够捕获字段及属性遗漏、内部字段遗漏、枚举差异、页面/角色/状态/模块遗漏和可可靠证明的权限反转。
5. 研发视角审查中产品规格阻塞问题为 0；技术设计问题允许由研发继续处理。
6. 现有 lint、consistency、artifact guard 和相关测试保持通过。

## 4. 明确不改

- 不改变 `decision-notes.md` 的定位和未决事项处理方式。
- 不要求 PRD 正文增加未决问题章节。
- 不修改 Design confirmation 机制。
- 不改变 PRD 与 Prototype 的并列下游关系。
- 不让 PRD 依赖 Prototype。
- 不引入 API、数据库、HTTP 状态码、加密算法等技术设计内容。
- 不将 ABC 分析过程写入最终 Design。
- 不执行 Git commit 或 Git push。

## 5. 完成定义

只有同时满足以下条件才算完成：

- Design 首次生成规则能够发现适用的高影响产品缺口；
- PRD 能够完整承接 Design 的研发必要事实；
- 字段、状态、权限、规则、场景和异常不再因模板压缩而系统性丢失；
- 可可靠证明的遗漏和冲突有确定性检查；
- 语义问题有 Review 检查项；
- 固定样例和现有回归测试通过；
- Design 仍保持人类可读和轻量，PRD 仍保持现有写作组织，技术设计边界未被突破。
