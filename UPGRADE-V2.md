# 从 main 升级到 V2，到底升级了什么

> 面向用户（产品经理）的版本更新说明。V2 是在 main 基础上继续迭代的版本（main 之后又经过 42 次提交迭代），10 个技能全部做了升级。

## 一句话结论

- 原型从"能看的静态页面"变成"能跑的标准工程"，评审会现场可以点着讲；
- PRD 从"看着像那么回事"变成"能直接上评审"，字段和设计逐项对得上；
- Design 从单一大文件变成多文件设计集（设计地图 + 设计集清单 + 系统级基线 + 跨模块契约 + 模块设计），每项产品事实只有一个归属处，改动只影响真正相关的模块；
- 改了 Design 里的字段/权限/状态，实际依赖它的 PRD / 原型模块会被标记受影响并同步，三份文档不会再互相打架；
- 上手更简单：新项目不用先建任何文件，也不用记内部流程依赖，不再需要"确认 Design"或确认哈希。

**流程骨架没有变**（聊需求 → 对齐 → 设计 → 校验 → PRD/原型），变的是每一步的质量和手感。

## 怎么升级（30 秒）

```powershell
cd D:\work\ShitPM   # 或你存放仓库的位置
git pull
```

装过的人什么都不用做，自动生效——安装是软链（junction），仓库一更新，宿主立刻能用到。想确认装没装好：

```powershell
python scripts/python/shitpm-host.py verify --host codex
```

只有换宿主、卸载重装时才需要重跑安装。技能清单还是那 10 个：启动、对齐、设计、PRD、原型、同步修复、三个 Review、原型标注——没有新增、没有删减。

## 这次升级了什么

### 1. 原型：从"纸片"到"能跑的工程"（改动最大的一项）

| | main（升级前） | V2（升级后） |
|---|---|---|
| 原型形态 | 单个 HTML 文件 + 网页在线库 | 标准前端工程（Vite + React + Ant Design 6 组件库） |
| 长相 | 样式简单，复杂页面力不从心 | 组件齐全（表格、弹窗、表单、菜单），接近真实系统 |
| 本地预览/上传 | 靠浏览器打开，上传另想办法 | 双击"原型工具.bat"：本地预览、构建、上传一条龙 |
| 依赖 | 需要联网下载页面库 | 工程自带依赖，构建结果可随时重建 |

另外原型写法也立了规矩：角色切换、表格、操作栏这些高频场景有了统一质量要求；标注版原型（带备注弹窗）直接基于工程复制，不污染原始原型。

**效果**：评审会上不再是一堆静态页面，主流程和状态变化能点出来；给研发看的时候，组件表达也更接近真实界面。

### 2. PRD：按业务闭环写、大文档分段写、落笔前三关

- **组织方式变了**：按"业务闭环 → 业务阶段 → 页面 → 动作"组织，不再是小模块层层嵌套，读起来更像人写的需求文档。
- **大 PRD 分段写入**：文档再长也是分段完成的，不会写到后面忘了前面、前后口径不一致。
- **正式落笔前三关**：对照 Design 逐项核对（模块/页面/字段/状态/权限一个不许超纲）；风格自检（专杀"点击按钮→填写表单→提交"流水账）；一致性校验（PRD 字段和 Design 对不上直接拦下）。
- **拿不准的不能悄悄拍板**：Design 里标"未决"的高影响问题，PRD 必须原样暴露，等你确认具体业务问题。
- **流程图交付升级**：draw.io 源文件 + 高清 PNG（2 倍分辨率），文档里直接能看。
- **吸收了 main 的资深写法**：主链路按操作顺序写、关键分支单列、下一步由谁继续、界面文案用引号标出，这些都并进了规则。

**效果**：PRD 能直接上评审会，研发不用追着你问"这没写清楚"。

### 3. Design：多文件设计集 + 文件级指纹溯源，锁住"唯一事实体系"

- **两种模式，只问一次**：简单需求走简单模式（最小闭环），复杂需求走完整模式（需求理解 → 业务建模 → 产品承接 + 跨层自检），AI 不擅自升级降级。
- **结构更规范**：页面 → 区块 → 字段/操作，字段表和操作表是唯一权威定义，其他宽表只做速览。
- **多文件设计集**：系统级事实、跨模块契约、模块内部事实分开承载，每项正式事实只有一个归属处；小模块任务只读取目标模块及必要基线、契约，不再每次全量读整套 Design。
- **文件级指纹溯源**：设计集清单登记每个正式文件的内容指纹，PRD / 原型生成时记录自己实际读取了哪些 Design 文件。Design 文件变化后，真正依赖它的下游模块会被标记受影响；"同步修复"一次连续完成 Design 修改与受影响下游的同步，不需要再执行"确认 Design"或确认哈希。
- **高影响未知只问具体业务问题**：无法安全决定的事实（如"库存不足时是否允许部分扣减"）在归属文件标记未决并问你，不要求你确认 Design 或选择文件。
- **大 Design 分段读取**：文档再大也不一次硬塞进上下文，避免"后半段失忆"。

**效果**："设计里 8 个字段、PRD 里写了 6 个"这种事故，从机制上杜绝。

### 4. 同步修复：改动有分寸

- 字段/权限/状态/模块边界这类**高影响修改**：先回写 Design 唯一事实归属 → 连续同步所有实际存在且受影响的 PRD / 原型模块 → 针对性检查 → 更新下游依据，无确认停顿。
- 纯文案、措辞、颜色这类**表现层修改**：只改对应文档，不动 Design。
- Design 没变、下游写错了：只修错误下游，不动 Design 和另一份下游。
- 外部未知修改：只把相关下游标记受影响，不自动改写。
- AI 不再自作主张：不自动创建不存在的下游；判断不了影响范围时按高影响处理，宁可多走一步。

**效果**：改一处不会带出三份打架的文档，AI 也不会越权替你拍板。

### 5. Review：回到"第二意见"的定位

- 三个 Review（设计/PRD/原型）都是按需独立挑战：只出问题清单、不改稿、不自动阻塞后续；Design Review 默认只审查目标模块闭包，完整 Review 由你明确触发。
- 删掉了原来一层中间的检查产物和部分检查脚本，流程更轻。

**效果**：想要第二意见就喊一声，不喊没人逼你；流程不再被固定门禁卡住。

### 6. 长项目不"失忆"

- 上下文装载改成确定性方案：按清单打包加载，预算可见。
- 配合 Design 多文件闭包装载，大项目（比如 50+ 页的设计）也能稳定处理到最后一页。

**效果**：项目越大越能感觉到差别——以前做一半发现 AI"忘了前面"，现在很少发生。

### 7. 上手更简单

- 新项目**不用预建任何文件**（不用 status.json、不用 metadata），直接"启动辅助器"就能开始。
- 启动时 AI 列出**所有当前能做的动作**，没有依赖的可以并行；每个动作还带模型建议（简单活用轻量模型，帮你省 token）。
- 固定八步门禁拆掉了：PRD 和原型是并列下游，想先要哪个要哪个，只要一个也行；不再有"确认 Design"这一步。

**效果**：三分钟上手，不用记内部依赖。

### 8. 清理与合规

- 移除了仓库内部品牌和内部开发规则对用户项目的干扰（用户项目不再加载 ShitPM 仓库自身的开发规则）。
- 删除了一批死资产和重复文档（旧 CDN 页面库、旧单文件原型壳、旧的 Design 确认脚本与模板等）。
- 补了开源许可证；本地测试资产与发布内容隔离。

## 升级前 vs 升级后，一张表看明白

| 场景 | main（升级前） | V2（升级后） |
|---|---|---|
| 原型 | 单文件 HTML，样式简单 | 标准组件工程，接近真实系统，一键预览/构建/上传 |
| PRD 写作 | 一次性写完，容易流水账、前后不一致 | 分业务闭环、分段写、三关自检，能直接上评审 |
| 改了 Design 字段 | 靠人肉记着去改 PRD/原型 | 文件级指纹溯源：依赖它的下游标记受影响，同步修复连续完成 |
| 拿不准的高影响问题 | 容易被 AI 悄悄拍板 | 必须标"未决"暴露给你，问具体业务问题 |
| 新项目启动 | 要准备 status.json 等内部文件 | 空项目直接开始，AI 列出可用动作 |
| Review | 偏门禁，可能卡流程 | 按需第二意见，不阻塞 |
| 大项目 | 上下文容易丢 | 多文件闭包装载 + 可控打包，稳定收尾 |
| 升级安装 | — | git pull 即生效，无需重装 |

## 说明

本说明基于 main → V2 之间的实际代码差异和 42 次提交整理，覆盖 10 个技能的全部升级。旧项目里如果有历史遗留的 metadata 之类的文件，它们不会成为新流程的障碍，也不算质量证明；新项目不再需要它们。旧项目的单体 design.md 可按迁移提示词转为多文件设计集后继续使用。
