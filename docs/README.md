# docs 历史归档说明

`docs/plans/`、`docs/reports/`、`docs/wiki/` 是本仓库的历史执行记录（计划、验收报告、对抗式审查、知识沉淀），仅用于追溯，**不属于 ShitPM 运行时规则**。

Agent 运行时规则以 `skills/`、`contracts/`、`references/`、`schemas/`、`templates/` 和 `USAGE.md` 为准；本目录内容无需读取。历史记录中提及的已删除组件（如 `design-orchestrator.py`、`.tmp-fusion/`）不代表当前仓库状态。
