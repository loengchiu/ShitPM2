# R16 对抗性审查报告：PRD Skill 最终优化落地 + SKILL 两层治理方案预审

> 日期：2026-08-04
> 审查对象：`docs/plans/2026-08-04-prd-skill-final-optimization-and-acceptance.md` 的执行落地（8 个文件，未提交）；`docs/plans/2026-08-04-skill-two-layer-governance-plan-and-acceptance.md`（计划文件，未执行）
> 基线：b9a4a0e（R15 之后的 HEAD）
> 方法：方案逐条核对落地 + 对抗性实证（指令-实现一致性、检查器兼容性、全量测试、真实项目 Design 冲突前提验证）

## 1. 结论

**第一份方案（PRD Skill 最终优化）落地质量高：0 P0 / 0 P1 / 5 P2。** 四约束进入 Skill 本体、格式统一彻底、无旧格式残留、指令与实现一致（R14 的 `--example` 静默失效雷区未复发）、7 套测试全绿、验收前提经真实项目验证属实。

**第二份方案（SKILL 两层治理：否定句正面化 + 完成判据）尚未执行。** 工作区 10 个 SKILL 中只有 spm-prd 动过，且改动全部是第一份方案的内容（四约束 + 格式 + 模块命令），无任何否定句/判据改动。本报告对第二份方案只做计划层面的预审。

## 2. 落地核对（方案 §6 逐项）

| 方案要求 | 落地情况 | 判定 |
|---|---|---|
| SKILL.md 写入四约束 | ✅ 三处交叉比较（A-1）、有效引用（D）、时间范围事实边界、最后写入后重验收（最终检查）均入正文 | 通过 |
| SKILL.md 补页面操作逐项承接 | ✅ 模块输入、模块完成条件第 10 项、生成内自检第 12 项 | 通过 |
| SKILL.md 补页面/动作/终端格式 | ✅ 独立"页面与动作格式"章节 + 自检第 13 项 | 通过 |
| SKILL.md 明确阶段 C 必须实际运行模块上下文命令 | ✅ "每个模块必须实际运行该模块上下文装载命令；不得跳过…" | 通过 |
| templates/prd.md 页面改 `######`、动作改 `**` | ✅ 含注释同步 | 通过 |
| prd-writing-rules.md 同步 | ✅ §3.1 页面承接、§5 格式、§9.1 交叉比较、§12 引用、边界 §6/§7 | 通过 |
| prd-scene-checklist.md 补自检项 | ✅ 目录 + 页面操作逐项承接/事实交叉一致性/引用与时间范围三节 | 通过 |
| prd-review-checklist.md 补 Review 项 | ✅ T1-T5（格式/承接/引用/时间范围/重验收） | 通过 |
| writing-examples.md 同步示例 | ✅ 两处旧示例改新格式，无相反示例残留 | 通过 |
| 测试同步 | ✅ test-prd-simplification 加断言、test-prd-style-lint 换新格式样本 | 通过 |
| manifest 仅核对不改 | ✅ prd-cards.scenes 已在 module pass 装载链，无需改 | 通过 |
| 禁止修改项（用户项目/其他 skill/commit/push） | ✅ 未越界 | 通过 |

## 3. 对抗性实证（本轮重点）

### 3.1 指令-实现一致性（R14 雷区排查）✅

方案要求阶段 C 必须实际运行 `context-pack.py --pass module --card scenes`。实测：

- 命令可跑，dry-run 正常输出 6 个 section（含 `prd-verification-scenes`）、404 行、5638 tokens；
- manifest 中 `module` pass → `prd-cards` → `card_sections.scenes` → `prd-verification-scenes`，装载链完整；
- 新增的"页面操作逐项承接/事实交叉一致性/引用与时间范围"三个场景自检节位于 `<!-- context:prd-verification-scenes -->` 标记内，会被真实装载。

R14 的 `--example` 静默失效（参数存在但 pack 不在装载链）本轮没有复发。

### 3.2 格式变更对确定性检查器的冲击 ✅

- `prd-consistency-check.py`：页面身份以总体"页面清单"表为权威来源（mapping 表存在时直接 return，不扫描正文标题），`###### 页面` / `**动作**` 新格式不会漏识别、也不会把动作误当页面；旧模板兼容分支保留，新旧格式并存可处理；
- `prd-style-lint.py`：无页面/动作格式规则（仅动作流水账、重复稳定标识），新格式无误判；
- `prd-consistency-check.py` 无 `加粗/######/（管理端）` 格式依赖。

### 3.3 全量测试 ✅

| 测试 | 结果 |
|---|---|
| test-prd-simplification | PASS（含新格式断言与旧格式禁止断言） |
| test-prd-style-lint | PASS |
| test-context-loading | PASS |
| test-shitpm-regression | PASS（3 用例） |
| test-prd-consistency-semantics | PASS |
| test-design-index | OK |
| test-anti-hallucination | 通过 |
| test-context-runtime | 冒烟通过 |

### 3.4 方案 §8.10 验收前提（Design 冲突）经真实项目验证属实 ✅

智慧停车区 `output/design/design.md` 实测：

- L252-253 状态表：`未处理 → 处理中`、`处理中 → 已处理`；
- L844 页面操作"确认处理"：`未处理变为处理中或已处理` + `备注缺失不能提交`；
- L602 备注字段：`选填`；L721 核查备注：`异常时必填，正常时选填`；L844 处理备注：`缺失不能提交`。

状态目标冲突（处理中 vs 处理中或已处理）与必填冲突（选填 vs 缺失不能提交）均真实存在，方案没有编造验收样本前提。**注意：这也意味着智慧停车 PRD 全量重新生成会被新约束正确阻断，存量 prd.md（旧格式）短期不会迁移——新旧格式并存是预期状态。**

## 4. 发现的问题

### P1（无）

### P2（5 项，均非阻断）

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| P2-1 | spm-fix 不引用新页面/动作格式规则 | `skills/spm-fix/SKILL.md` | spm-fix 修改/新增 PRD 内容时无格式约束，传播链可能在新格式 PRD 上引入旧格式（页面加粗/动作六级标题）。方案 §6.3 未将 spm-fix 列入必改/核对范围，属边界模糊。当前影响小（存量 PRD 为旧格式，fix 保持现状是合理最小改动），但新格式 PRD 上线后需留意。建议：方案下轮在 spm-fix 中补一句"修改 PRD 时遵循 prd-writing-rules 的页面/动作格式"。 |
| P2-2 | "最后写入后重新验收"无确定性兜底 | `skills/spm-prd/SKILL.md` 最终检查 | 顺序规则（写入→回读→检查→改则重来→更新 status.json）完全依赖模型自觉。ShitPM 执行纪律已实证"模型自觉遵守流程假设证伪"，而这条恰是流程中最易被跳过的环节（先更新状态再改 PRD）。方案明确禁止新增检查器，故这是设计取舍而非缺陷，但应在验收中作为已知风险保留观察。 |
| P2-3 | 新旧格式长期并存 | 智慧停车存量 prd.md vs 新规则 | 存量 PRD 动作用六级标题（旧格式），检查器多模板兼容可处理；但 spm-prd-review 的 T1 格式检查（P2 级）若审查旧 PRD 会报格式差异。旧 PRD 已交付不再重审，风险低。迁移策略需用户明确：存量不迁移 vs 择机全量重生成。 |
| P2-4 | 两层治理方案未执行 | 全部 9 个非 prd SKILL | 计划合理（纯措辞、不增程序、与"skill 是行为协议"定位一致），但尚未落地。执行时注意：判据草案（方案 §5.2）基于改动前 SKILL 文本撰写，需与第一份方案落地后的实际文本对齐后再写。 |
| P2-5 | "硬护栏占比 ≥80%"目标无依据 | 两层治理方案 §10.1 | 80% 是拍脑袋数字。建议改为"剩余否定句均可证为无法正面化的硬护栏"这类可判定的标准，避免执行时为了凑比例误删硬护栏或漏改可正面化句。 |

## 5. 观察（非问题）

- test-prd-simplification 用字符串断言钉死四约束关键短语，措辞微调即挂——这是方案 §8.1 要求的静态同步验收（建议 rg + 人工阅读），设计可接受。
- 方案 §4.1 的"纯浏览器行为、无业务差异且 Design 未定义业务结果的控件不要求扩写"豁免，存在被模型当偷懒借口的空间；Review T2 与自检第 12 项可拦截，暂不需改。
- 智慧停车 Design 除已识别的状态/必填冲突外，还有 L85/L1109 的待确认项（滞留结束展示名称"已处理"vs"已核实/已结束"），与新约束的阻断逻辑一致，无新增问题。

## 6. 与上轮（R15）遗留的关系

R15 的 P1（`--example` 静默失效）已按方案 A 修复（SKILL 删 `--example` 改 `--card scenes`），本轮实证 `--card scenes` 真实装载，闭环确认。

## 7. 建议（按优先级）

1. 落地第二份方案（两层治理）时，先与第一份方案落地后的 SKILL 文本对齐判据草案；
2. 下轮方案把 spm-fix 的格式引用缺口（P2-1）纳入修改范围；
3. 明确存量旧格式 PRD 的迁移策略（P2-3）：建议短期不迁移，智慧停车 Design 冲突修复并重新生成时自然切换到新格式；
4. 两层治理方案的 80% 目标改为可判定标准（P2-5）。

## 8. Git 状态

- 本次审查只读，未修改任何文件（除本报告）；
- 工作区 8 个改动文件 + 4 个未跟踪文档均未提交；
- 未执行 commit / push。
