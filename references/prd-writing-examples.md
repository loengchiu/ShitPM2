# PRD 写作示例

> 以下示例只说明组织和表达方式，业务事实、页面、字段、角色、状态、数字和提示语必须回到设计集清单登记的正式 Design 文件。示例中的名称、枚举、阈值、刷新周期和提示语均为占位演示，不得直接复制为真实需求。
>
> 使用方式：写作规范见 `references/prd-writing-rules.md` 详细需求说明写作规范；本文件按页面类型提供颗粒度对照。module pass 自动装载简单只读和多角色状态示例，分别展示篇幅下限与复杂页面的完整颗粒度；其他类型用 `--example <键>` 按需装载，不把全部示例常驻上下文。
>
> 表达分工：自然段承接业务前提、用户判断、系统处理、结果和下一环节；数字编号列表表达有先后顺序的步骤、状态推进和处理链路；`·` 并列列表表达同层并列的展示项、校验项、限制项、异常项和动作。每个示例只按内容需要选择表达，不为了展示排版强行使用全部三种。

## 目录

- [动作复杂度规范示例](#动作复杂度规范示例)
- [简单只读页面](#简单只读页面)
- [数据看板或多区块页面](#数据看板或多区块页面)
- [列表、详情和查询页](#列表详情和查询页)
- [普通表单或配置页](#普通表单或配置页)
- [多角色、多状态处理页](#多角色多状态处理页)
- [移动端任务和跨终端衔接页](#移动端任务和跨终端衔接页)
- [外部系统数据接入或自动动作](#外部系统数据接入或自动动作)
- [直接数据与基础字段页面](#直接数据与基础字段页面)
- [汇总统计与多维度报表](#汇总统计与多维度报表)
- [周期结算与财务清分](#周期结算与财务清分)

## 动作复杂度规范示例

### 简单动作（以“查看车位明细”为例）

**查看车位明细**

服务区管理员在车位监控页点击任意车位卡片，打开车位明细抽屉。抽屉只读展示车位编号、车位类型、当前停放车辆车牌号、入场时间与停放时长；未停放车辆时展示“空闲”并隐藏车辆关联信息。无数据或网络超时提示重试，关闭抽屉返回车位监控页并保留当前筛选范围。

---

### 普通动作（以“配置车辆滞留阈值”为例）

**保存滞留配置**

服务区管理员完成各车型滞留时间与通知开关修改后，点击页面底部“保存配置”。系统校验每一行输入：滞留时间必填且必须为 1 至 1440 之间的正整数。任意一行不合规，在该行下方标红提示具体错误并阻止提交，页面完整保留已录入内容。

全行校验通过后，系统持久化该服务区配置，记录修改人与修改时间，并在页面顶部提示“配置已更新”。新阈值从下一次车辆入场或状态轮询时生效，不追溯已生成的滞留告警；保存失败时保持当前表单内容可编辑并提示重试。

---

### 复杂动作（以“提交滞留核实结果”为例）

**提交滞留核实**

保安在现场核实表单中选择核实结果并补充信息后，点击“提交核实”并在二次确认弹窗中确认提交。提交前，系统按下列规则逐项校验。

· 核实结果为必填单选（正常、异常、未核实）；
· 选择“异常”时，处理备注必填，最多允许输入 200 个字符；
· 驾驶员手机号若填写，必须符合标准 11 位手机号格式，校验不通过在对应字段下方提示并停留在当前页。

提交时系统校验当前告警仍处于“核实中”状态。校验通过后，告警状态推进为“已结束”，写入核实结果、现场补录信息、处理人及提交时间，页面返回移动端通知列表。

若提交时流程已被其他人员处理或车辆已离场，系统拒绝覆盖已有结果，弹窗提示“当前告警状态已变更，请刷新查看”，已填写内容暂存于表单供保安参考，不产生重复记录。管理端告警列表同步更新为“已结束”并挂载该次处理记录。

<!-- context:prd-example-simple-readonly:start -->
## 简单只读页面

纯查看页面写清入口、查看对象、关键展示内容、数据来源、查看范围、无数据和返回目标；没有表单和状态变化时不扩写。

### 查看车位占用

###### 车位占用情况页

服务区管理员从车流监控页进入车位占用情况页，查看当前服务区各类型车位的已用数量、总数和占用率。已用数量来自车位监测识别结果，总数来自服务区车位档案；占用率按已用数量除以总数计算并保留一位小数，不高于 50% 显示绿色、超过 50% 且不高于 80% 显示橙色、超过 80% 显示红色。车位无数据时显示“暂无数据”，与确实为零区分；单个车位识别异常仅该车位显示“数据异常”，不影响其他类型统计。页面不提供筛选和导出，返回车流监控页。
<!-- context:prd-example-simple-readonly:end -->

<!-- context:prd-example-dashboard:start -->
## 数据看板或多区块页面

多区块页面从业务任务开始，写清区域构成、区域共享范围、指标口径、刷新规则、局部失败、范围切换和旧数据隔离。

### 实时监测当前服务区运行

###### 车流实时监控页

服务区管理员进入页面确认当前服务区是否正常运转：通过指标判断进出规模，通过在场分布定位异常车辆，通过视频确认现场情况。页面由服务区选择与实时状态、指标卡片、在场车辆分布、视频监控和车流统计五个区域构成，全部区域共用当前选择的服务区范围，切换服务区后五个区域一起刷新，不保留上一服务区数据。

· 指标卡片展示今日入场、今日离场、当前在场车辆和车位占用率：前三项按自然日 0 点至当前时间统计当前服务区的累计和在场数量，占用率按已用车位除以总车位数计算并保留一位小数；
· 在场车辆分布按小车位、大车位、危化品车位切换，展示车位编号、车牌号、车型、入场时间、停放时长和两客一危标识，超过滞留阈值的车辆橙色高亮，存在两客一危异常或未处理告警的车辆红色标识；
· 视频监控展示已绑定监控点，每个监控点一个播放器卡片，支持播放、暂停、静音、全屏和截图，截图保存到本地且不改变业务记录；
· 车流统计展示各车型在场数量、平均/最长/最短停靠时间及 24 小时进出场和在场趋势，平均停靠时间只按有完整入离场记录的车辆计算。

所有指标和列表每 30 秒自动刷新，刷新时保留上一次成功数据，仅在对应区域显示加载状态；单个区域请求超过 30 秒提示超时并提供重试，不影响其他区域；断网时提示“网络已断开，部分数据可能无法更新”。车型无数据时显示“暂无数据”，趋势无数据时显示“暂无趋势数据”，不得显示为零。

**定位异常车辆**

管理员按车位类型切换查看范围后，系统展示该类型下的在场车辆；点击任意车辆卡片打开车辆详情，记录已不存在时提示刷新并返回。无权限服务区在下拉中不可选，上级角色可查看车辆级明细但不显示处理入口。
<!-- context:prd-example-dashboard:end -->

<!-- context:prd-example-list-detail-query:start -->
## 列表、详情和查询页

列表页写清列表对象、关键列、默认范围、实际存在的筛选和排序、空数据、行操作、进入详情和返回后的条件保留；详情页写清查看对象、内容来源、只读边界和返回。

### 查询并查看车辆信息

###### 车辆信息记录页

服务区管理员进入车辆信息记录页，默认查看当前服务区全部车辆记录，按入场时间倒序展示。列表展示车牌号、车牌颜色、车型、入场时间、当前停车状态和关联的两客一危标记；有未处理告警的记录整行标红。

· 筛选区支持按车牌号模糊查询、按车型下拉选择、按停车状态下拉选择和按入场日期范围查询，所有条件可组合；
· 无匹配记录时显示空态提示，不展示空表头；查询失败提示重试并保留上一次成功列表；
· 每页 20 条，支持翻页；排序仅支持入场时间正序或倒序。

点击任意记录进入车辆详情页，详情页展示车辆基本信息、两客一危信息、进离场和停车信息及状态追踪，所有字段只读。返回列表时保留查询条件、排序和分页位置；记录在查看期间被其他入口改变时，详情页提示“记录已变化”并提供刷新。

**导出当前列表**

服务区管理员、区域中心和服务中心可导出当前筛选结果，导出格式为 Excel，单次不超过 1 万条；超过上限提示缩小时间范围。导出完成后自动下载，并生成包含导出人、导出时间和导出范围的导出记录；导出失败提示重试。
<!-- context:prd-example-list-detail-query:end -->

<!-- context:prd-example-form-config:start -->
## 普通表单或配置页

表单和配置页写清已有值来源、输入方式、必填和校验、字段联动、保存或提交对象、生效结果、失败保留及对历史和关联对象的影响。

### 配置车辆滞留阈值

###### 滞留阈值配置页

服务区管理员按车型配置滞留阈值和通知开关，页面默认带出当前服务区各车型的既有配置。配置项按车型分行，每行包含车型（只读）、滞留时间（分钟，必填，正整数）和通知开关（必填，开或关）。

· 滞留时间允许输入 1 至 1440 分钟，超出范围或非数字时在该行下方提示并阻止保存；
· 通知开关关闭时，该车型超过阈值不产生滞留通知；
· 车型未配置时显示默认值占位，配置保存后才生效。

保存时系统校验所有车型行：任意一行校验失败则不保存任何配置，已填写内容保留；校验通过后写入该服务区的车型滞留配置，替换原配置并记录操作人和操作时间。配置结果从下次车辆停放时长达到阈值时开始生效，不影响已产生的滞留流程；保存失败保留全部已填写内容并提示重试。
<!-- context:prd-example-form-config:end -->

<!-- context:prd-example-multi-role-state:start -->
## 多角色、多状态处理页

多角色、多状态页面写清详情展示、角色可见和可编辑差异、各状态下操作入口、状态变化、处理记录、并发冲突和下一责任人。

### 处理滞留核实

###### 滞留告警详情与处理页

保安从移动端滞留通知进入“待核实”告警详情，服务区管理员从管理端告警列表进入同一详情。页面展示告警时间、车牌号、车型、车牌颜色、停放车位、入场时间、停放时长和滞留时长，这些字段始终只读；车辆为两客一危或待核查时，额外展示危险品类型、驾驶员、联系电话和所属公司，普通超停车辆不展示这些字段。

管理端对该页面全部内容只读，仅查看处理留痕；移动端保安按现场核实权限可更新危险品类型、驾驶员、联系电话和所属公司，联系电话需符合手机号格式。页面按滞留流程状态驱动操作入口：

· 待核实状态显示“开始核实”，保安点击后流程进入核实中，系统记录开始处理人和时间；
· 核实中状态显示“更新相关信息”和“提交核实”，保安可修改允许更新的字段并填写处理结果（必填单选：已核查车辆正常停靠、已通知驾驶员驶离、已上报上级部门、其他情况）和处理备注（选填多行）；
· 已结束和已离场状态仅展示历史，不提供任何处理入口。

保安提交核实前需二次确认，确认后系统校验流程仍处于核实中：校验通过则结束当前滞留流程，状态变为已结束，留存处理人、处理时间、处理结果和处理说明，页面返回通知列表；流程已被他人处理或车辆已离场时，系统不覆盖原结果，提示查看最新状态后重新决定，已填写内容保留。服务区管理员在告警列表中看到该告警状态同步为已结束并可查看处理时间线。
<!-- context:prd-example-multi-role-state:end -->

<!-- context:prd-example-mobile-cross-terminal:start -->
## 移动端任务和跨终端衔接页

移动端页面区分任务列表、任务详情和处理表单，写清进入和返回关系、现场输入、弱网保留、草稿和提交差异、任务状态以及管理端或其他角色如何接收结果。

### 现场核查两客一危车辆

###### 两客一危核查任务（移动端）

保安从待核查任务列表进入“两客一危核查任务”，列表按待核查优先、任务时间倒序展示车牌号、车辆照片、入场时间、停车位置、两客一危类型、危险品类型、任务状态和任务时间；停车位置未识别显示未知，无照片显示占位图。返回列表时保留排序。

待核查任务显示“接收任务”入口，保安点击后任务进入核查中并打开核查表单；任务已被其他保安领取时提示刷新，不进入表单。核查表单由只读的车辆信息区和可编辑的核查区组成：

· 只读区展示车牌号、车辆照片、入场时间、停车位置；
· 核查区“是否两客一危”为必填单选（是、否、待核查），选择“是”后展示两客一危类型下拉（长途客运、旅游运输、危化品运输）并可修改，危化品运输时展示危险品类型下拉并可修改；
· 驾驶员姓名、驾驶员手机号、所属公司、公司电话为选填可修改文本输入，手机号和公司电话格式不正确不能提交；
· 核查结果为必填单选（正常、异常、未核实），核查备注为多行输入，核查结果选择异常时备注必填。

保安可随时保存草稿：保存成功保持核查中状态，弱网或保存失败时保留本地未提交内容并提示重试，不视为已提交。提交前需二次确认，提交成功后退回任务列表，任务状态变为已完成，核查结果和补录字段写入车辆两客一危信息并生成一条核查台账记录；管理端两客一危车辆列表和台账页可看到更新后的核查状态和补录内容。提交失败不改变任务状态，已填写内容保留可重试。
<!-- context:prd-example-mobile-cross-terminal:end -->

<!-- context:prd-example-external-auto:start -->
## 外部系统数据接入或自动动作

自动动作把 Design 自动动作表（触发条件、成功结果、失败结果、状态保留、用户反馈、人工处理）融入自然语言，写清触发来源、输入数据、系统使用方式、成功结果、失败后的对象状态、用户反馈、人工处理和后续恢复。

### 自动生成车辆入场记录

卡口摄像头识别车辆后向系统推送车牌号、车牌颜色、车辆照片、车身颜色、车型和进场时间。系统接收后校验关键字段，按以下规则推进处理：

· 关键字段校验通过后，系统创建车辆停车记录并进入停留中状态，同时更新当前服务区在场车辆数和车位占用信息；
· 车牌若无法识别，系统创建识别异常记录并保留抓拍图片与时间，不阻塞其他车辆正常进场；
· 关键字段若缺失，系统创建数据异常记录，保留已接收数据并等待管理员核实或后续数据补齐。

车辆属于或疑似两客一危时，系统在进场记录生成后向集团自建库发起比对：比对成功生成核查任务，查询失败进入比对失败状态，不阻塞车辆进出，比对失败可重试或由保安人工核查。异常车辆在车辆信息记录列表可见对应异常状态；服务区管理员负责人工标注、关联或核实。自建库不返回驾驶员姓名、手机号和归属公司时，移动端核查页不把这些字段显示为自动获取值，由保安现场补录。
<!-- context:prd-example-external-auto:end -->

<!-- context:prd-example-direct-data:start -->
## 直接数据与基础字段页面

适用于第一档直接数据（单源字段直接回显、单表计数或基础字段展示）。写清权威数据来源对象与属性，以及无数据或空值时的回显展示；无需强套复杂时间起止、聚合公式或结算模板，保持篇幅精炼。

### 查看车辆基础信息

###### 车辆基础档案页

服务区管理员进入车辆基础档案页查看指定车辆的静态备案档案。页面所有字段均为第一档直接数据，严格自底层业务实体单源回显：

· 车辆核心标识：车牌号（取自 `VehicleRecord.plate_no`）、车牌颜色（取自 `VehicleRecord.plate_color`）、车辆所有人（取自 `VehicleRecord.owner_name`）；若所有人为空，前端统一展示为“`-`”；
· 登记元数据：备案登记时间（取自 `VehicleRecord.register_time`，格式为 `YYYY-MM-DD HH:mm:ss`）、登记经办人（取自 `SysUser.real_name`）；
· 状态标识：当前运营状态（取自字典 `vehicle_status`，展示为对应状态标签）。

本页面所有数据只读，不设派生换算或时间区间聚合；无对应车辆档案时展示“未查询到该车辆备案信息”空状态提示并提供返回入口。
<!-- context:prd-example-direct-data:end -->

<!-- context:prd-example-aggregate-data:start -->
## 汇总统计与多维度报表

适用于第三档汇总统计数据。遵循模块自包含原则：`4.x.5` 收敛公共统计口径九项要素；`4.x.6` 页面承接规则并定义交互；`4.x.7` 仅保留控件属性与来源索引（严禁口径压缩在字段表）；同名指标在卡片、列表、图表、下钻和导出中严格同口径。

### 服务区车流综合统计

#### 4.1.5 状态与业务规则

1. **服务区当日车流量与在场统计口径**：
   · 统计对象为出入场车辆流水记录（`ParkingRecord`）；
   · 组织范围限定为当前所选服务区所属范围，严格继承操作员组织数据权限；
   · 时间边界为当日自然日 `00:00:00` 至 `23:59:59`（按当前系统时间所在自然日闭区间计算）；
   · 分子与分母界定中，“今日车位占用率”分子为当前在场车辆总数，分母为该服务区核定总车位数（`ParkingLot.capacity`），若总车位数为 0 则展示为 `0.0%`；
   · 纳入与排除规则为仅统计状态为“已进场”“已离场”的有效业务车辆，显式排除测试车牌（含“TEST”前缀）及系统作废单据；
   · 去重规则为“累计车流量”按车牌号 + 入场时间唯一流水统计且不去重，“今日服务车辆数”按车牌号去重计数；
   · 刷新与时效为页面每 60 秒定时自动增量轮询刷新，支持手动点击“立即刷新”；
   · 同名一致性约束为顶部指标卡“今日累计车流”、车流趋势图汇总值、明细列表汇总行以及导出 Excel 默认必须严格采用上述完全一致的范围与排除条件，下钻列表直接继承当前筛选范围。

###### 车流统计分析页

服务区管理员进入车流统计分析页，宏观掌控服务区运行规模与车流负载。页面由顶部指标卡、车型分布饼图、24小时车流趋势图和下表明细构成。

· 顶部指标卡：展示“今日累计车流”“当前在场车辆”“车位占用率”三项指标，指标口径严格承接 4.1.5 规则 1；
· 车流趋势图：按小时颗粒度聚合入场与出场曲线，无车流时对应时段显示数值 0；
· 下钻明细：点击指标卡或趋势节点，打开侧边抽屉展示对应时段车辆流水明细列表，筛选条件与排序默认继承主页面上下文。

#### 4.1.7 字段定义

| 字段 | 类型/取值 | 来源或约束 | 使用说明 |
|---|---|---|---|
| 今日累计车流 | 数值（整数） | 系统计算（口径见 4.1.5 规则 1） | 顶部卡片展示，千分位显示，只读 |
| 当前在场车辆 | 数值（整数） | 系统计算（口径见 4.1.5 规则 1） | 顶部卡片展示，千分位显示，只读 |
| 车位占用率 | 百分比（保留1位小数） | 系统计算（口径见 4.1.5 规则 1） | 顶部卡片展示，超 90% 标红预警，只读 |
<!-- context:prd-example-aggregate-data:end -->

<!-- context:prd-example-periodic-calculation:start -->
## 周期结算与财务清分

适用于第四档周期结算与复杂计费。`4.x.5` 明确账期锚点、取数版本快照、分步公式、过程与最终精度、角分尾差归属、重算锁定；`4.x.7` 仅保留字段来源索引；`4.x.9` 必须提供包含具体数值的正常例与边界例。

### 商铺租金月度清分

#### 4.2.5 状态与业务规则

1. **月度租金清分与按天折算规则**：
   · 计费账期与时间锚点设定计费周期为自然月（每月 1 日 `00:00:00` 至当月最后一日 `23:59:59`），每月 1 日凌晨 2 点启动上月清分批次作业；
   · 取数版本策略以结算启动时已审核生效的租赁合同快照（`LeaseContractSnapshot`）为唯一权威依据，结算中途合同变更计入下期调整；
   · 分步计算公式中，月基础租金 = 合同约定月租金基数 × 营业提成系数；实际生效天数 = 统计周期内商铺有效营业天数（排除免租期）；日折算租金 = 月基础租金 / 当月实际自然天数（28/29/30/31天）；应清分租金 = 日折算租金 × 实际生效天数；
   · 过程精度与最终精度要求日折算租金计算中间过程保留 4 位小数，最终清分金额及账单金额四舍五入保留 2 位小数（精确到分）；
   · 角分尾差平衡机制明确多方分摊或日折算总和与月基础租金之间的角分尾差，统一在结算周期的最后一期由出租方统一吸收，确保子项之和严格等于总租金；
   · 重算与锁定规则明确生成结算单并经财务主管审核确认后，当前账期数据生成不可变历史快照并硬锁定，严禁直接修改或重算；如遇退租争议或合同补录，通过次月跨期调整单进行金额冲正。

#### 4.2.7 字段定义

| 字段 | 类型/取值 | 来源或约束 | 使用说明 |
|---|---|---|---|
| 月基础租金 | 金额（元） | 系统计算（口径见 4.2.5 规则 1） | 列表与详情展示，保留2位小数 |
| 日折算租金 | 金额（元） | 系统计算（口径见 4.2.5 规则 1） | 详情抽屉展开展示，保留4位小数 |
| 应清分租金 | 金额（元） | 系统计算（口径见 4.2.5 规则 1） | 结算单主金额，保留2位小数 |
| 尾差调整额 | 金额（元） | 系统计算（口径见 4.2.5 规则 1） | 尾差调节项，保留2位小数 |

#### 4.2.9 业务验收与用例

1. 清分计算正常例（标准自然月场景）
输入条件为合同月租金基数 10,000.00 元，营业提成系数 1.0，结算月份 2026 年 3 月（共 31 天），有效营业天数 31 天。
推导计算中月基础租金 10,000.00 元，日折算租金 10,000 / 31 = 322.5806 元，未调尾差总额 322.5806 × 31 = 9,999.9986 元四舍五入为 10,000.00 元，尾差调整额 0.00 元。
预期输出为最终应清分租金 10,000.00 元。

2. 清分计算边界例（首月不满周期与闰年 2 月场景）
输入条件为合同于 2028 年 2 月 15 日生效，月租金基数 29,000.00 元，2028 年为闰年 2 月共 29 天，当月实际生效天数 15 天（2月15日~2月29日）。
推导计算中日折算租金 29,000.00 / 29 = 1,000.0000 元/天，实际生效天数 15 天，应清分租金 1,000.0000 × 15 = 15,000.00 元，尾差调整额 0.00 元。
预期输出为最终应清分租金 15,000.00 元，系统无尾差报错。
<!-- context:prd-example-periodic-calculation:end -->
