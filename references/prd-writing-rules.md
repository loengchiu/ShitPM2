# PRD 写作规则

> 本文件的“详细需求说明写作规范”是 `4.x.6 功能详细说明` 的唯一规范源；`templates/prd.md` 只提供骨架，`references/prd-writing-examples.md` 提供表达对照，确认版 `output/design/design.md` 是产品事实源。
<!-- context:prd-writing-structure:start -->
## 最小结构规则（writing pass 装载）

1. PRD 顶层顺序：文档信息 → 产品概述 → 总体说明 → 功能需求 → 待确认事项。
2. 功能需求按业务闭环或业务结果组织，不按菜单、页面清单、管理端/移动端或 Design 章节机械复制。
3. 每个有页面或业务动作的功能模块必须包含 `4.x.6 功能详细说明`，不能以“无内容”为由省略；没有页面和业务动作的内容不得创建为功能模块。
4. 管理端和移动端放在同一闭环的真实业务阶段；共用页面只定义一次，其他模块说明适用范围。
5. 页面标题、动作标题和终端后缀的格式见 `templates/prd.md`。
6. 模块存在跨页面推进、跨角色协作、跨系统交互、关键业务状态变化，或会改变业务结果的分支、异常与恢复时，`4.x.4 业务流程` 必须包含流程图；简单只读、纯展示或无状态变化和协作分支的配置模块不强制画图。
<!-- context:prd-writing-structure:end -->
<!-- context:prd-writing-spec:start -->
## 详细需求说明写作规范（module pass 装载）

本节是 `4.x.6` 的唯一规范源。交付目标：研发只读 `4.x.6`，不查看 Prototype、Design 或旧 PRD，也不回问产品，即可确定产品行为并独立开发。前端据此确定页面形态、区域、内容、操作和状态；后端据此确定数据来源、处理对象、状态变化和下游结果；测试据此编写主链路与异常用例。视觉风格、像素尺寸、组件技术选型不属于本节职责，除非影响业务判断且 Design 已确认。Design 只作为生成时的事实输入，不能成为交付 PRD 的外部引用。

### 1. 自然语言硬约束
`4.x.6` 正文全部使用自然语言，按内容场景选择三种表达，且三种表达分工正确：

- 自然段用于业务承接，连续表达业务前提、用户判断、系统处理、结果和下一环节；
- 数字编号列表用于有先后顺序的步骤、状态推进和处理链路，每一项必须是完整业务语句；
- `·` 并列列表用于同层并列的展示项、校验项、限制项、异常项和动作，每一项说明条件、含义或结果。

页面标题和动作标题继续用于定位正文。页面标题必须逐字采用 Design 页面清单和页面定义中的页面名称，不得按动作、业务结果或页面内容自行改名、合并改名或新造名称；一个闭环承接多个 Design 页面时，分别使用各自的原页面名称，合并时在正文中说明承接关系。以下写法不得作为正文：字段表、页面区块表、操作表、状态表或数据流表；“页面职责、使用对象、入口与返回、页面区块、字段来源”等固定元数据块；“触发、输入、处理、成功结果、失败结果、后续去向”等行首标签；“字段名：说明”“来源：某系统”“结果：已完成”等键值对句式；读者跨多个表格、章节或上游文档才能拼出的一条业务链；同一套句式机械替换页面名和字段名。

### 2. 按实际适用覆盖
写作前先在内部判断前端和后端各项事实是否适用。适用项必须在正文有明确落点；不适用项直接跳过，不创建空段落，不写“本页不涉及”。无法判断且影响核心流程、权限、数据范围、状态、写入对象或外部协作的事项，必须作为高影响未知回到 Design，不由 PRD 或研发自行拍板。判断不生成适用性表格、检查回执或中间 JSON。

### 3. 前端事实承接
页面实际存在的能力必须通过自然语言写清，不能因为 Prototype、Design 其他章节或常见研发经验中可能存在而省略：

- 页面形态与边界：读者能判断是看板、列表、详情、表单，还是多个页面组合；属于同一业务动作的多个页面写清如何进入、返回和衔接。
- 页面构成与信息关系：写清承担业务判断或操作的区域、区域之间共用的查看范围/筛选条件/当前对象，以及切换范围后哪些内容同步变化。
- 展示内容与方式：页面实际展示的字段、指标、状态、图片、视频、趋势、历史记录和提示在正文可读；展示方式影响业务判断或操作时写清是文本、状态标签、图片、播放器、卡片、列表、趋势图、单选、多选、下拉还是多行输入。
- 字段来源与编辑性：动态字段写清业务来源（用户输入、系统计算、当前页面上下文、业务对象关联、外部系统返回或其他角色补录）；写清何时只读、何时可编辑、由哪个角色编辑、已有值如何带出、什么条件下显示或隐藏，以及空值、未知值、超长和异常值如何呈现。
- 操作入口与状态驱动展示：写清用户从哪里发起操作，按钮或入口在什么角色、权限和业务状态下显示、隐藏、可点或不可点；状态变化后相关按钮、字段、列表项、状态标签和页面内容如何刷新或消失。
- 页面过程状态：加载中、空数据、加载失败、无权限、离线、部分数据失败或外部资源不可用时，写清用户看到什么、已成功数据是否保留、能否重试，以及部分失败是否影响其他区域。
- 导航与状态保留：写清页面入口、成功后停留/跳转/返回、返回目标，以及返回后筛选、排序、分页、滚动位置或未提交内容是否保留。
- 多终端与多角色：同一业务在管理端、移动端、小程序等存在不同页面和操作时，分别写清每个终端展示什么、允许修改什么、完成后如何衔接其他角色；不能用“管理端只读、移动端可操作”一句话替代。

### 4. 后端事实承接

业务实际存在的能力必须写清，让后端能确定业务数据从哪里产生、按什么范围和规则查询计算校验、操作后修改哪个业务对象、哪些字段和状态变化、产生哪些记录，以及结果进入哪些后续环节：

- 数据来源与权威来源：每项关键动态数据写清来自哪个业务对象、内部计算、用户录入、组织权限、设备、外部系统或其他角色处理结果；多个来源可能提供同一字段时，写清哪个是当前权威值、人工修改是否覆盖外部值、后续外部更新是否再次覆盖。
- 查询、范围与计算规则：查询和统计写清对象、组织、时间、状态、筛选继承、纳入排除、去重和计算口径；共用范围的指标、列表、趋势、下钻和导出保持一致；刷新失败、切换范围和缓存并存时，写清保留当前范围旧数据还是清空并显示失败，不留展示上一组织数据的歧义。
- 用户输入与业务校验：写清已有值如何带出、哪些字段必填、允许什么取值、字段间联动或互斥关系、在什么时点校验，以及校验失败后页面和已填写内容如何处理。
- 系统处理与写入对象：保存、提交、修改、确认、绑定、领取、撤回、删除、作废或归档时，写清修改哪个业务对象、哪些字段和状态变化、是否更新主信息、当前任务、业务快照、关联对象或历史记录，以及多个对象同时变化时的业务关系和先后结果。
- 新增记录与审计留痕：动作生成任务、台账、告警、处理记录、导出记录、修改历史或审计记录时，写清何时生成、记录哪些产品事实、由谁查看、生成失败是否影响主动作；“记录操作日志”不能替代具体业务用途和下游可见结果。
- 状态变化与重复执行：强动作写清允许发起的角色和状态、成功后的目标状态、状态变化后哪些动作不再允许；同一任务可能被多人处理、重复提交或处理期间被外部事件改变时，写清是否拒绝覆盖、用户看到什么、已填数据是否保留、下一步如何继续。PRD 不规定锁或幂等，但必须规定冲突后的业务结果。
- 下游影响与后续消费者：动作完成后写清结果出现在哪些列表、详情、台账、统计、告警或其他角色待办中，以及数据变化对后续判断、统计口径、权限可见性或外部同步的影响；只写“保存成功”或“返回列表”不构成完整结果。
- 外部系统与失败边界：外部系统提供数据时，写清谁提供、本系统如何使用、失败后业务对象保持什么状态、用户看到什么，以及由系统重试、用户重试、人工处理还是等待外部恢复；只有 Design 已确认的重试次数、超时、补偿和恢复方式才能写成事实。

### 5. 动作组织与标题

Design 操作表是事实输入，不是 PRD 动作目录，动作按业务结果重组：无独立业务结果的浏览动作（查询、筛选、重置、返回、切换、播放控制等）按业务结果合并；动作标题用“动词 + 业务对象/结果”，不得把“点击、打开、切换、返回、播放”直接当标题，不为了证明承接而一对一复刻。动作按复杂度写：简单查询、查看、返回写触发条件与业务结果；普通表单、保存、提交、修改写前置条件、输入与校验、系统处理、成功结果和失败保留；多角色、多状态、多分支、破坏性或外部协作写允许角色和状态、关键分支、状态变化、失败恢复和后续衔接。合并或精简后不丢失角色、前置条件、状态、数据范围、失败结果和后续去向。

### 6. 跨前后端的完整业务链

页面和动作最终通过自然语言连续表达：用户在什么业务前提、角色和状态下进入页面，需要完成什么判断或任务；页面向用户提供哪些信息，这些信息来自哪里；用户如何操作，系统依据什么规则处理；成功后哪些对象、字段、状态和页面发生变化；失败后保留什么、用户如何继续；结果由哪个角色、页面或业务环节继续使用。不要求固定段落数量，但不缺少实际适用且影响开发的事实。

### 7. 事实边界与信息密度

Design 是唯一事实源，PRD 只承接已确认事实，不静默新增页面、字段、角色、权限、状态、流程、金额口径和外部系统行为；权限、数据范围、状态机、删除传播、外部协作、历史数据、统计口径等未知保留为待确认并回到 Design，不用“按配置”“按业务规则”替代；状态条件、动作分支或权限依据存在多种可能且 Design 未拍板时，禁止用“或”“按业务规则”“按配置”等模糊表达保留不可执行分支，必须回到 Design；不得从“立即生效”等生效描述推导 Design 未确认的时间范围。不写背景原因、价值判断和空泛目标；不重复默认角色和全局规则；不用“支持相关操作”“按常规处理”“用于承载”等模糊句；不为显得完整补造排序、分页、按钮、超时或提示；少用加粗；列表不强行嵌套。

### 8. 业务流程图

模块写作前按当前 Design 片段判断是否命中流程图条件。出现跨页面推进、跨角色协作、跨系统交互、关键业务状态变化，或会改变业务结果的分支、异常与恢复时必须画图；不能因为正文已经写成步骤而省略。只有纯查询、纯展示，或无状态变化、无角色交接、无外部协作和关键分支的简单配置可以只写文字。

每个命中模块至少一张图；复杂模块按独立闭环拆分，不把整个系统塞进一张巨图。一张图只表达一个明确业务闭环，必须能看出起点、参与角色、业务动作、关键判断、主要分支、异常出口和结果状态；节点使用 Design 与当前 PRD 已有的业务名称，不写接口、数据库、消息队列等技术实现，除非它们属于已确认的产品边界。

流程图在模块正文写清后生成，保存为 `output/prd/diagrams/<图名>.drawio`，并按 2 倍分辨率导出同名 `.png`。PNG 就近嵌入对应模块的 `4.x.4 业务流程`，`.drawio` 作为可编辑源文件随 PRD 一起交付。流程图只辅助理解，正文仍必须完整写清规则；图不得新增正文或 Design 没有的产品事实。

### 9. 统计页与复杂动作逐页验收

含统计、看板、趋势、下钻或导出的模块，必须按页面或子闭环分片生成，不得一次压缩多个统计页。每个 `4.x.6` 页面完成前确认以下四项，任一缺失先回到 Design，不进入下一页面：

- 页面标题与 Design 页面名称完全一致；Design 页面已落正文，或正文明确写出被合并的原页面名称及其承接关系（合并必须写清原页面承接的业务结果去向，不留丢失）；
- 页面形态、区域、关键字段和操作入口可确定；
- 数据来源、组织范围、时间范围和统计/校验口径可确定（统计页必须能读出统计对象、组织范围、时间边界、分子分母、纳入排除、去重、空值零值、下钻继承和导出范围）；
- 状态、权限、异常、成功结果和下一环节可确定。

全部模块生成完成后，从最后一个模块向前回读至少两个模块，专门检查是否出现摘要化和事实密度下降；发现后半段退化为指标、按钮或字段摘要时立即重写该模块并重新回读。

`prd-consistency-check.py` 出现 `deterministic_conflict` 时，不得标记为“全绿”或人工放行；必须修复正文消除冲突，或先修正检查器本身并重新运行。
<!-- context:prd-writing-spec:end -->

<!-- context:prd-core-boundary:start -->
## 事实边界（writing / module pass 装载）

1. 唯一事实源是确认版 `output/design/design.md`；Prototype、旧 PRD、示例和通用经验只能帮助理解，不能覆盖或补造事实。
2. 高影响未知（权限、数据范围、角色、状态机、流程、删除/作废/归档传播、外部系统、历史数据、统计口径）必须列为待确认并回到 Design；低影响展示细节只能承接 Design confirmation 已拍板的推断值。
3. Design 冲突不得由 PRD 拍板：保留冲突事实、说明冲突位置和影响对象、列为待确认，并在无法确认前停止对应模块写作，建议回到 Design Fix。
4. 生成后直接回读真实 `prd.md` 与 Design：检查承接、遗漏和擅自增加，发现问题直接修正；不生成承接矩阵、覆盖率报告、验证回执或机器签名替代回读。
5. 交付 PRD 必须自包含。不得写 `Design §x.x`、`output/design/design.md`、`按 Design 规则` 或其他跨文件指针；确认后的规则直接写入当前 PRD，重复使用时只引用当前 PRD 已存在的章节或页面。未确认事实只写“待确认”及影响，不用外部引用掩盖缺口。
6. 流程图是 PRD 交付包内的辅助资产，不替代正文事实。图中的角色、动作、判断、分支、异常和结果必须能在当前 PRD 正文中定位。
<!-- context:prd-core-boundary:end -->
