{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReviewResult",
  "description": "review 结果机读 JSON（文档性契约：当前无脚本强制校验此文件，仅作为 review 结果 JSON 的结构约定；如需强制校验请接入 jsonschema 执行路径）",
  "type": "object",
  "required": ["stage", "verdict", "issues", "issue_layer", "affected_objects", "needs_upstream_sync", "reviewed_at"],
  "properties": {
    "stage": {
      "$ref": "common.schema.json#/$defs/review_stage_enum",
      "description": "被 review 的阶段"
    },
    "verdict": {
      "$ref": "common.schema.json#/$defs/verdict_enum",
      "description": "review 结论，只允许三档"
    },
    "issues": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "severity", "description"],
        "properties": {
          "id": { "type": "string" },
          "severity": { "type": "string", "enum": ["P0", "P1", "P2"] },
          "description": { "type": "string" },
          "location": { "type": ["string", "null"] },
          "suggestion": { "type": ["string", "null"] }
        },
        "additionalProperties": false
      },
      "description": "问题列表"
    },
    "issue_layer": {
      "type": "object",
      "description": "问题归属层分布",
      "required": ["structure", "content", "consistency"],
      "properties": {
        "structure": { "type": "integer", "description": "结构性问题数" },
        "content": { "type": "integer", "description": "内容质量问题数" },
        "consistency": { "type": "integer", "description": "一致性问题数" }
      },
      "additionalProperties": false
    },
    "affected_objects": {
      "type": "array",
      "items": { "type": "string" },
      "description": "受影响对象列表（稳定 ID 或对象名）"
    },
    "needs_upstream_sync": {
      "type": "boolean",
      "description": "是否需要回上游同步修复"
    },
    "next_recommended": {
      "type": ["string", "null"],
      "enum": ["align", "design", "design-review", "prd", "prd-review", "prototype", "prototype-review", "fix", "done", null],
      "description": "下一步建议（vNext 可选，不再线性推进；不输出或输出 null 均合法）"
    },
    "reviewed_at": {
      "type": "string",
      "description": "ISO 8601 时间戳"
    },
    "metadata_generated": {
      "type": "boolean",
      "description": "是否已生成 metadata（仅 design review 第三段写入，其他阶段不写）"
    }
  },
  "additionalProperties": false
}
