<!-- context:design-module-template:start -->
# [模块名] 模块设计

> 本文件承载只影响一个模块内部的事实：模块流程、页面、区块、字段、操作、局部状态、规则、异常和验收。
> 系统级事实见系统级基线目录，跨模块交接见跨模块契约目录。

## 一、模块职责与边界

<!-- 说明模块解决什么、不做什么；与相邻模块的交接只引用契约，不复制契约正文。 -->

## 二、模块业务闭环

<!-- 每个关键闭环按"触发 → 参与者 → 主要过程 → 分支 → 结果 → 恢复 → 后续责任"组织；简单闭环可用短段落，不强制制造空表格。 -->

### [业务闭环名称]

#### 流程速览

<!-- 触发条件、参与角色、主要阶段、成功结果、不可逆点和后续责任。 -->

#### 分阶段动作与分支

<!-- 每个关键动作写：角色、前置条件、输入、判断条件、产品动作、对象/字段变化、成功结果、失败结果、状态变化、下一步和后续责任。 -->

#### 异常、回退与补偿

<!-- 按实际场景覆盖唯一性冲突、重复提交、并发、网络/超时、外部失败、部分成功、驳回、撤回和中止。 -->

#### 相关规则

<!-- 只写该闭环的规则；跨模块共性规则在契约或系统基线。 -->

## 三、模块局部对象、规则与状态

### 影响业务结果的规则

<!-- 写清适用范围、判断条件、例外和可观察结果。 -->

### 关键对象生命周期与状态

<!-- 每个有状态流转的实体用独立标题开始（如 ### 状态机：整改任务）。每张表六列：状态、含义、操作人、触发动作、下一状态、限制条件。没有真实状态变化时写"无状态机"。 -->

## 四、页面、区块、字段与操作设计

<!-- 本章是 PRD 和 Prototype 的页面事实基线。页面清单中的每个页面都必须在下方完整展开；不得只列代表页面或代表字段。 -->

### 页面清单（可选速览）

| 页面 | 用户任务 | 适用角色 | 主要入口/去向 |
| --- | --- | --- | --- |
|  |  |  |  |

### 页面：[页面名称]

- 页面 ID：
- 所属端和菜单层级：
- 入口和返回目标：
- 页面目的：
- 适用角色：
- 核心数据对象：
- 进入条件：
- 数据范围：
- 主要状态：没有真实状态变化时写"无状态机"。
- 加载、空、异常和无权限状态：
- 页面展示行为：按需写加载中/骨架屏、空态、异常态、无权限入口、超长文本、默认值、标签/颜色、分页/排序和筛选条件保留。
- 状态驱动展示：按状态写可见操作、置灰操作及原因、隐藏操作、字段显隐、行点击/跳转、标签/颜色和状态变化后的刷新时机。
- 页面完成标准：

#### 区块：[区块名称]

- 区块目的：

##### 字段表

| 字段名称 | 业务含义 | 字段来源 | 展示条件 | 输入与编辑规则 | 取值与默认规则 | 交互方式 | 校验与反馈 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |

<!-- 每个产品字段单独一行，不得把多个字段合并为一行。用户可见、可编辑、可筛选、可搜索、作为列表列展示、作为统计指标或被页面操作直接依赖的字段都必须列出。同一页面可继续增加区块。 -->

#### 页面操作

<!-- 页面存在用户操作时，先写 #### 页面操作 + 一行区块目的，再用 ##### 操作表 列出十列：操作、适用角色、入口/触发方式、输入（字段级）、展示与可用条件、是否二次确认、成功结果、数据与状态变化、失败与恢复、后续去向。"是否二次确认"只填 是/否；"后续去向"按业务闭环填写，无后续去向写"无"。两列均不得填"待确认"。 -->

##### 操作表

| 操作 | 适用角色 | 入口/触发方式 | 输入（字段级） | 展示与可用条件 | 是否二次确认 | 成功结果 | 数据与状态变化 | 失败与恢复 | 后续去向 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |  |  |

### 非页面落点字段（按需）

| 字段 | 不在页面出现的原因 |
| --- | --- |
|  |  |

## 五、模块内外部协作与异常处理

<!-- 只在存在其他系统、组织或人工环节时保留；写清事实来源、同步方向、延迟、失败/部分成功、补偿方式和最终责任。 -->

## 六、成功与验收

<!-- 写可观察的业务结果和关键场景验收，不展开成研发测试用例。 -->

| 场景/前置条件 | 用户操作或系统事件 | 可观察结果 | 覆盖的规则/目标 |
| --- | --- | --- | --- |
|  |  |  |  |

## 七、模块内未决与排除项

<!-- 高影响未知直接写在本文件：状态、影响范围、当前保守表达、需要谁确认。排除项说明不实现原因。 -->
<!-- context:design-module-template:end -->
