# 设计基线

> 本文档是产品、研发和测试共同使用的唯一 Design 事实基线。
> 按业务闭环组织内容，不按内部分析步骤组织目录。
> 下列标题只提供可选骨架：没有适用事实时删除该标题，不保留空章节，不用“不适用”堆砌形式完整。

## 一、目标、范围与边界

<!-- 写清：为什么做、解决什么问题、预期结果、成功标准、范围内、范围外、依赖、约束。 -->

### 目标与成功标准

<!-- 目标必须可判断；不要只写“提升效率”“支持管理”。 -->

### 范围内与范围外

<!-- 范围外必须能阻止下游把排除项重新实现。 -->

### 系统边界与跨系统责任

<!-- 仅在存在其他系统、组织或模块责任交接时保留；写清谁提供什么、谁负责失败处理。 -->

## 二、角色、数据范围与责任

<!-- 按业务责任组织，不按技术角色或数据库表组织。 -->

### 角色定义

<!-- 写清职责、可执行动作、不可执行动作和责任边界。 -->

### 数据范围

<!-- 涉及多级组织时，写清每个角色的可见层级、页面差异、字段差异和落点差异。 -->

## 三、业务对象与关系

<!-- 只定义影响流程、规则、权限或数据结果的核心对象及关系；字段细节放字段定义。 -->

## 四、业务闭环

<!-- 每个关键闭环使用一个小节，按触发 → 处理 → 分支 → 结果 → 异常组织。 -->

### [业务闭环名称]

#### 流程速览

<!-- 多角色或 3+ 步骤必须写角色和阶段。单角色、无分支流程可用一段话写清触发、结果和不可逆性。 -->

#### 分阶段动作与分支

<!-- 每步写角色、前置条件、输入、判断条件、系统动作、对象/字段变化、成功/失败结果、状态及副作用、下一步和后续责任。 -->

#### 异常、回退与补偿

<!-- 按实际场景写清唯一性冲突、网络/超时、重复提交、并发、外部失败、部分成功、驳回、撤回和中止的产品结果、恢复方式、历史影响与责任人。 -->

#### 相关规则

<!-- 只写该闭环的业务规则；按需覆盖生命周期、时间口径、文件/导入导出、批量、列表默认和产品级质量约束；跨闭环共性规则放“共性规则”。 -->

## 五、系统承接

<!-- 只保留本需求实际需要的内容；没有对应业务事实的空小节删除。 -->

### 模块与页面清单

<!-- 用表格写页面、所属业务闭环、主要任务、可见数据范围和关键动作。 -->

| 页面 | 所属闭环/模块 | 主要任务 | 可见范围 | 关键动作 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 字段定义

<!-- 页面可见、可编辑、可筛选或动作依赖的字段必须有页面落点。纯内部字段在非页面落点表说明原因。 -->

| 字段 | 类型 | 长度 | 必填 | 默认值 | 枚举值 | 格式 | 业务来源 | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |  |

### 页面与字段落点

<!-- 按页面 > 区域/动作 > 字段组织；字段名直接引用上表。 -->

#### [页面名称]

| 区域/动作 | 字段 |
| --- | --- |
|  |  |

### 非页面落点字段

<!-- 只有纯内部字段可以放这里，并说明不在页面出现的原因；没有则删除本小节。 -->

| 字段 | 原因 |
| --- | --- |
|  |  |

### 权限与敏感操作

<!-- 先写页面默认权限，再写角色例外、数据范围和敏感动作保护。没有角色差异时可并入角色定义。 -->

#### [页面或闭环名称]

| 默认规则/角色 | 可见范围 | 可执行动作 | 字段权限例外 | 敏感操作限制 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 六、规则与状态

### 共性规则

<!-- 只放跨闭环适用的规则；每条规则写适用范围和验收方式。没有共性规则则删除。 -->

### 状态定义

<!-- 有状态流转的实体必须使用一张完整 6 列表：状态、含义、操作人、触发动作、下一状态、限制条件。非终态有出路，非初始态有入路，终态占行。 -->

#### [实体名称]

状态流转路径：`[初始状态] → [中间状态] → [终态]`

| 状态 | 含义 | 操作人 | 触发动作 | 下一状态 | 限制条件 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

<!-- 无状态变化的实体必须明确写“无状态机”，不要创建空状态表。 -->

## 七、验收条件

<!-- 按业务结果写可观察、可执行的验收条件，覆盖主路径、分支、规则、权限和异常。 -->

| 编号 | 场景/前置条件 | 操作 | 预期结果 | 覆盖规则 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 八、待确认事项

<!-- 只列仍会改变方案的高影响问题；每项说明影响范围。没有待确认事项写“无”。 -->

| 问题 | 影响范围 | 当前保守表达 | 需要谁确认 |
| --- | --- | --- | --- |
|  |  |  |  |

## 九、方案选择与权衡

<!-- 仅在确实存在方案分歧或取舍时保留；写采用方案、理由、未采用方案和代价。没有则删除。 -->
