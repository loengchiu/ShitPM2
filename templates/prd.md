<!-- context:prd-template:start -->
# PRD 正文

## 1. 文档信息

| 版本 | 日期 | 修订人 | 修订说明 | 状态 |
|---|---|---|---|---|
| v1.0 | YYYY-MM-DD | （生成人） | 首次生成，基于确认版 Design 展开 | 评审稿 |

- 事实源：`output/design/design.md`；如存在 Design confirmation，以确认内容为准。

## 2. 产品概述

<!-- 说明产品目标、服务对象、本期范围、明确不在范围内的内容和关键限制。没有确认事实的项不要自行补全。 -->

### 2.1 产品目标

### 2.2 目标用户

### 2.3 本期范围

### 2.4 非本期范围

## 3. 总体说明

<!-- 本章只放跨模块共用且只需定义一次的内容。页面映射只用于定位，不替代功能模块正文；页面正文必须在功能模块的 4.x.6 功能详细说明中落点。 -->

### 3.1 业务概述

### 3.2 业务对象与关系

### 3.3 角色与权限

### 3.4 公共规则

### 3.5 页面清单

| 页面/入口 | 终端 | 所属业务闭环 | 主要承接阶段 | 备注 |
|---|---|---|---|---|

### 3.6 术语定义

<!-- 术语只定义一次；详细格式参见 references/prd-glossary-format.md。 -->

### 3.7 全局待确认事项

<!-- 只列跨模块事项；模块内事项写在所属模块末尾。本节不重复定义。 -->

## 4. 功能需求

<!-- 按业务结果或业务闭环组织，不按菜单、页面目录或终端拆分。模块数量和名称以实际 Design 为准。每个有页面或业务动作的功能模块必须包含 4.x.6 功能详细说明；没有页面和业务动作的内容不得创建为功能模块。 -->

### 4.1 功能模块名称

#### 4.1.1 功能概述

<!-- 写清本模块要完成的业务结果、起点、终点、包含内容和不包含内容。只写模块摘要不满足交付，页面和动作必须落到 4.1.6 功能详细说明。 -->

#### 4.1.2 业务对象与关系

<!-- 只写本模块新增或重点使用的对象；共用对象引用第 3 章的权威定义，并说明本模块的使用关系。 -->

#### 4.1.3 角色与权限

<!-- 说明角色、组织范围、可见数据、可执行动作和字段级例外；没有事实依据不要补充角色或权限。 -->

#### 4.1.4 业务流程

<!-- 一张图只讲一个闭环。需要流程图时交付 draw.io 源文件和按 2 倍分辨率（X2）导出的 PNG，并在此引用；不使用 Mermaid。 -->

#### 4.1.5 状态与业务规则

<!-- 区分业务状态、页面展示状态和操作过程状态；说明状态变化的触发条件、允许动作和异常边界。 -->

#### 4.1.6 功能详细说明

<!-- 功能模块必需章节，不能以“无内容”为由省略；没有页面和业务动作的模块不得创建。按“业务阶段/业务结果”组织页面和动作。管理端、移动端应放在同一闭环的实际阶段，不单独设“移动端模块”。阶段、页面标题直接写业务名称，不要加“阶段一/二”“页面：”等标签前缀。页面不只有名称，还要写页面职责、使用对象、所属业务侧、所处业务阶段、入口/返回目标和区块业务目的；动作写清角色、前置条件、输入、处理、成功结果、状态影响、失败结果和恢复方式。 -->

##### 4.1.6.1 业务阶段名称

<!-- 阶段标题直接写阶段业务名称，如：进场采集与记录生成（系统自动）。每个业务阶段至少能回答：谁在什么业务状态下进入、完成什么业务结果、成功与失败边界。 -->

###### 页面名称

<!-- 页面固定使用六级标题，如：###### 车辆信息记录。PC 管理端页面名称不加“（管理端）”“（PC端）”“（终端）”后缀；只有移动端、小程序、自助终端等特殊终端保留终端标识，如：###### 待核查任务（移动端）。不要加“页面：”前缀。页面正文说明页面职责、使用对象、所属业务侧、所处业务阶段、入口/返回目标、页面区块及区块业务目的，并展开该页面上的可执行动作。 -->

页面职责和使用对象。

<!-- 页面区块以列表呈现，不加"页面区块与业务目的："标题；每条格式：区块名：承载字段或操作，业务目的。 -->
- 筛选区：服务区名称、处理状态、时间范围、车牌号、车型，用于定位记录；
- 列表：车牌号、车型、入场时间、停放时长、告警时间、处理状态、处理人、处理时间、操作，用于查看和处理记录。

<!-- 展示行为以·列表呈现，不加"页面展示行为和状态驱动展示："标题；按 Design 已确认事实逐条列出（无权限、加载中/骨架屏、空态、异常态、超长文本、默认值、标签/颜色、分页/排序、筛选条件保留、状态驱动操作、字段显隐、点击跳转、刷新时机）；未确认的长度、颜色或刷新时机列为待确认，不补造数字。 -->
·时间快捷项为今日、近7天、近30天、自定义；
·默认按告警时间倒序；支持按停放时长和处理时间排序；
·每页20条；查询失败提示重试；
·上级角色只读，不显示处理、配置、上报按钮。

**动作名称**

用结构化自然语言说明触发条件、输入和字段、处理规则、成功结果、状态影响、失败结果、异常与恢复、权限和数据范围。每个动作正文必须能回答交互四问：入口（从哪个按钮/链接/菜单/通知触发，与页面状态驱动展示一致）、表单（输入字段构成：可编辑/必填/取值/格式，引用字段定义）、反馈（成功/失败时界面显示什么：提示、保留数据、跳转/停留、恢复方式）、分支与确认（多出口动作写清各分支；提交/删除/导出/作废等需二次确认的动作写明确认；失败/异常分支写界面处理）。Design 未定义交互细节时列为待确认，不补造按钮、表单或反馈。出现自动动作时补充触发、失败后的状态保留、用户反馈和人工处理或待确认；出现删除类动作时补充关联传播和恢复/审计或待确认。动作固定使用单独一行加粗，如：**查询车辆记录**；动作按独立业务结果拆分，不写操作流水账；动作正文是功能详细说明的主体，不能被字段表或模块摘要替代。

#### 4.1.7 字段定义

<!-- 字段按业务对象或阶段就近定义。可以使用局部表格，但不要求固定列数；字段属性必须以 Design 已确认内容为准。非页面落点字段必须进入对象字段定义并说明内部用途；高影响内部字段原则上一字段一行；合并字段不能掩盖不同对象、不同属性或不同权限。页面字段表只补充展示、编辑、筛选、只读、来源和校验等使用差异，不能替代 4.1.6 的动作正文。 -->

##### 业务对象名称

| 字段 | 类型/取值 | 来源或约束 | 使用说明 |
|---|---|---|---|

#### 4.1.8 异常处理

<!-- 说明外部系统、数据责任、成功/失败边界和已确认的恢复方式；未知的超时、重试、补偿不要静默补造。 -->

#### 4.1.9 验收标准

<!-- 验收写业务结果、关键状态、权限和异常结果，放在所属模块内，不在文末统一汇总。 -->

#### 4.1.10 待确认事项

<!-- 只列本模块的高影响未知和需要用户决策的事项；可以保守表达的低影响未知不强行升级。 -->

<!-- 复制 4.1 作为其他功能模块的组织参考。模块完成前不能跳过 4.1.6 功能详细说明、异常处理、验收标准和待确认事项；不为形式保留空章节，也不以“无内容”为由省略已有模块的必需章节。 -->

## 5. 待确认事项

<!-- 只做定位索引，引用全局或模块内的权威事项，不新增结论、不重复定义。 -->

| 编号 | 事项 | 所在位置 | 影响范围 | 处理方式 |
|---|---|---|---|---|

<!-- context:prd-template:end -->
