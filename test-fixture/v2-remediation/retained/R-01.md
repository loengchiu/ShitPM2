# 保留样例 R-01：跨组织资产调拨

某资产管理系统需要支持服务区管理员发起资产调拨，区域管理员审核，调入服务区管理员确认。审核驳回后允许申请人修改并重新提交；调入确认成功后调出方数量扣减、调入方数量增加，双方台账和履历必须一致。调拨数量不能超过可用数量；网络超时不能造成重复扣减。运营部可查看全部服务区，服务区管理员只能查看本服务区。

本需求用于完整模式，重点观察：多角色责任、数量对象一致性、回退重提、跨组织数据范围、幂等与补偿。
